"""Work 2 analog KV-cache and LLM hybrid utilities.

Imports are intentionally lazy in downstream modules so documentation tooling can
inspect this package even when PyTorch or Transformers is not installed.
"""
