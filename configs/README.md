# configs/

Future home for reusable configuration files.

Only stable configs should live here. One-off experiment configs belong under `experiments/configs/`; archived or superseded configs belong under `archive/` with a manifest.

Current cleanup did not move any configs into this directory.
