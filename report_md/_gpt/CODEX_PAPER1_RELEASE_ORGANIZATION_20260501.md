# Codex Paper-1 Release Organization — 2026-05-01

## Verdict

Codex directly organized the Paper-1 release/reviewer materials. No DS handoff is needed for this stage.

No destructive git operation was used. No files were deleted except the temporary bundle/tarball generated by Codex in this same turn before rebuilding it cleanly.

## Created / Updated

### Canonical JSON Evidence

Created:

- `paper/latex_gpt/source_data/canonical_json/`
- `paper/latex_gpt/source_data/canonical_json/README.md`
- `paper/latex_gpt/source_data/canonical_json/manifest_canonical_json_20260501.csv`
- `paper/latex_gpt/source_data/canonical_json/manifest_canonical_json_20260501.json`

Content:

- 44 copied canonical JSON evidence files.
- No missing source files.
- Covers IdealDevice 8-bit baseline, pure 4-bit collapse, Ensemble HAT 4-bit 3-seed, and PCM UnitCell 8/6/4-bit three-seed fresh/drift/training-history artifacts.

Purpose:

- Allows public/reviewer release to exclude large `.pt` checkpoints without losing locked-number evidence.

### Reproducibility README

Created:

- `README_REPRODUCIBILITY_PAPER1.md`

Contains:

- quick verification commands;
- LaTeX build commands;
- canonical evidence paths;
- locked-number/protocol paths;
- explicit `.pt` exclusion policy;
- scope boundary excluding Remote 105/107 from Paper-1 requirements.

### Protocol Corrections

Updated:

- `outputs/CANONICAL_NUMBERS_FROZEN_20260430.md`
- `paper2_aihwkit_baseline/PCM_PROTOCOL.md`

Correction:

- Replaced the overstrict/false implication that every canonical PCM run used the same `--early-stop-patience 0` CLI.
- Current accurate statement: all canonical PCM ladder artifacts completed the intended 100-epoch schedule; most used patience 0; `r11d_6bit_pcm_seed789` recorded `early_stop_patience=10` but did not trigger early stop and logged 100 epochs. Per-run `training_history.json`/provenance is the source of truth.

No locked numerical values were changed.

### DS Checklist Corrections

Updated:

- `report_md/_gpt/DS_REVIEWER_BUNDLE_PLAN_20260501.md`
- `report_md/_gpt/DS_GITHUB_CLEAN_BRANCH_CHECKLIST_20260501.md`

Correction:

- Fixed script paths from `scripts/check_*.py` to `scripts/_gpt/check_*.py`.
- Added warning to preserve canonical JSON evidence when excluding `.pt` checkpoints.

### Reviewer Bundle

Created clean staging directory and tarball:

- `release_artifacts/paper1_reviewer_bundle_20260501_1645/`
- `release_artifacts/paper1_reviewer_bundle_20260501_1645.tar.gz`

Size:

- staging directory: 47 MB
- tarball: 42 MB

Included:

- Paper LaTeX source, main/SI PDFs, bibliography, figures, and source data.
- Canonical JSON evidence under `paper/latex_gpt/source_data/canonical_json/`.
- Verification scripts under `scripts/_gpt/`.
- Curated AIHWKit training/eval scripts and PCM protocol.
- Root reproducibility README, environment, requirements, license, locked-number table.

Excluded and verified absent:

- `.pt` checkpoint weights;
- `report_md/` internal logs;
- `.codex` internal files;
- LaTeX build junk (`*.aux`, `*.log`, `*.out`, `*.fdb_latexmk`, `*.fls`);
- backup files (`*.bak`);
- agent markdown (`*_gpt.md`).

Bundle manifest:

- `release_artifacts/paper1_reviewer_bundle_20260501_1645/MANIFEST_SHA256SUMS.txt`
- `sha256sum -c MANIFEST_SHA256SUMS.txt` passes.

## Verification Run

| Check | Result |
|---|---:|
| `python scripts/_gpt/check_locked_numbers.py` | PASS, 22/22 |
| `python scripts/_gpt/check_local_pcm_precision_ladder.py` | PASS |
| `python scripts/_gpt/audit_latex_bib_keys.py` | PASS, missing key count 0 |
| `python scripts/_gpt/audit_bib_doi_resolution.py` | PASS, 67/67 resolved or DOI-redirected |
| LaTeX/source grep for undefined refs/cites, fatal errors, overfull, placeholders, TODO/FIXME/TBD | PASS, no hits |
| Bundle forbidden-file scan | PASS, no hits |
| Bundle tarball forbidden-file scan | PASS, no hits |
| Bundle SHA256 manifest self-check | PASS |

## Remaining Work

Paper-1 GPU remains closed. Remaining local work is optional release hardening:

1. Decide whether to push a clean GitHub branch or only keep the tarball.
2. If pushing, stage from the curated bundle or copy its file list into a clean branch. Do not stage the dirty worktree wholesale.
3. If a venue requires full figure source data for every legacy SI conceptual diagram, recreate the two unresolved conceptual diagrams as TikZ/SVG. This is not a numerical blocker.
4. Wait for Remote 107 corrected rerun before making any Work-2 Analog KV-cache claim.
