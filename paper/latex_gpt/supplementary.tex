% !TeX root = supplementary_main.tex
\section{Supplementary Information}
\label{sec:supplementary}

\renewcommand{\thefigure}{S\arabic{figure}}
\renewcommand{\thetable}{S\arabic{table}}
\setcounter{figure}{0}
\setcounter{table}{0}

\subsection{Extended Methods}
\label{subsec:extended-methods}

\subsubsection{Analog/Digital Operator Mapping}
\label{subsec:supp-operator-mapping}

Table~\ref{tab:supp-operator-mapping} provides the full operator-level mapping rationale for the hybrid deployment.

\begin{table}[ht]
\centering
\caption{\textbf{Analog/digital operator mapping rationale.}}
\label{tab:supp-operator-mapping}
\small
\begin{tabular}{@{}ll>{\raggedright\arraybackslash}p{6.5cm}@{}}
\toprule
\textbf{Operator} & \textbf{Domain} & \textbf{Rationale} \\
\midrule
Patch embedding (conv) & Analog & High weight reuse, regular access pattern; natural fit for crossbar MVM \\
QKV projections & Analog & Dense matrix-vector multiplications; weights stationary during inference \\
Attention output proj. & Analog & Post-attention linear projection; same characteristics as QKV \\
Feed-forward networks & Analog & MLP layers dominate parameter count; efficient crossbar utilization \\
\midrule
Depthwise convolutions & Digital & Low weight reuse, irregular spatial access; poor crossbar utilization \citep{wu2023bwq} \\
Layer normalization & Digital & Requires mean/variance computation across features; hard to parallelize on crossbar \\
$\mathbf{QK}^{\top}$ (attention scores) & Digital & Dynamic, input-dependent computation; irregular memory access \citep{lin2024hardsea} \\
Softmax & Digital & Non-linear transcendental function; precision-critical; retained in the digital path in contemporary hybrid CIM ViT mappings \citep{kim2025hemlet,lin2024hardsea} \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Weight-to-Conductance Mapping Pipeline}
\label{subsec:supp-weight-mapping}

Weights are decomposed into differential conductance pairs, quantized, perturbed by the device profile, and recovered via differential readout.

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.98\textwidth]{fig2_weight_mapping}
    \caption{\textbf{Weight-to-conductance mapping pipeline.} Digital weights are quantized, mapped to differential conductance pairs, perturbed by D2D/C2C device terms, accumulated in the analog domain, digitized, and recovered with a layer-wise scale factor.}
    \label{fig:supp-weight-mapping-pipeline}
\end{figure}

\subsubsection{Hybrid System Architecture}
\label{subsec:supp-system-architecture}

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.84\textwidth]{fig1_system_architecture}
    \caption{\textbf{Hybrid organic-CIM inference stack.} Dense weight-stationary operators are executed on analog crossbars, whereas dynamic attention and control-heavy operators remain in the digital path.}
    \label{fig:supp-system-architecture}
\end{figure}

\subsubsection{Nonlinear-Write Surrogate}
\label{subsec:supp-nl-surrogate}

In the surrogate model, nonlinear write modifies the STE backward pass according to the present conductance state:
\begin{equation}
    \frac{\partial L}{\partial G}
    =
    \begin{cases}
        \dfrac{\partial L}{\partial \tilde{G}}
        \left(\dfrac{G_{\max}-G}{G_{\max}-G_{\min}}\right)^{NL_{\mathrm{LTP}}-1},
        & \dfrac{\partial L}{\partial \tilde{G}} \ge 0 \\[10pt]
        \dfrac{\partial L}{\partial \tilde{G}}
        \left(\dfrac{G-G_{\min}}{G_{\max}-G_{\min}}\right)^{|NL_{\mathrm{LTD}}|-1},
        & \dfrac{\partial L}{\partial \tilde{G}} < 0
    \end{cases}
\label{eq:supp-nl-surrogate}
\end{equation}
The absence of an explicit $NL$ prefactor in Eq.~\ref{eq:supp-nl-surrogate} is intentional: the surrogate scales the STE by the normalized conductance position, modeling state-dependent update difficulty (weaker updates near the bounds), rather than by the derivative of a power-law function $f(G)=G^{NL}$.
This is therefore a behavioral proxy for position-dependent gradient attenuation, not a strict functional derivative.
When \texttt{use\_second\_order\_ste=True}, a second-order Taylor correction adds a negative-signed brake to both branches, preventing aggressive bound-pushing without changing the first-order scaling.\footnote{Branch~A ratified in canonical commit \texttt{ab56c2d}.}
Ideal write is recovered when $NL_{\mathrm{LTP}}=1$ and $|NL_{\mathrm{LTD}}|=1$.

\subsection{Detailed Experiment Matrix}
\label{subsec:detailed-experiment-matrix}

Tiny-ViT experiments span a digital baseline, a quantized hybrid control, a fixed-mask noisy deployment baseline, a uniform-noise HAT model, and several physical-extension studies. For brevity, these regimes are denoted V1--V8 throughout the Supplementary Information. V3 uses fixed D2D masking, analogous to ConvNeXt C3, while V7--V8 represent early retention-aware explorations excluded from the main analysis.

Four physical-extension studies probe richer device regimes on CIFAR-10:
\begin{itemize}[leftmargin=1.4em,itemsep=2pt,topsep=2pt]
    \item \textbf{Proportional-noise HAT} (\texttt{V4\_proportional\_HAT}) repeats V4 for \(100\) epochs with state-dependent proportional noise and uses \(10\)-run Monte Carlo sweeps under both proportional and uniform-noise semantics.
    \item \textbf{Nonlinear-write HAT} (\texttt{V4\_NL2\_HAT}) repeats V4 for \(100\) epochs with nonlinear-write dynamics active during STE gradient computation (\(\mathrm{NL}_{\mathrm{LTP}}=+2.0\), \(\mathrm{NL}_{\mathrm{LTD}}=-2.0\)) and uses the same \(10\)-run Monte Carlo evaluation.
    \item \textbf{Ensemble HAT} (\texttt{V4\_Ensemble\_HAT}) keeps the canonical V4 recipe but resamples the fixed D2D realization at each epoch; transferability is then reported on \(10\) fresh hardware instances with \(5\) Monte Carlo evaluations per instance.
    \item \textbf{ConvNeXt proportional-noise HAT} (\texttt{C4}) applies the same proportional-noise HAT protocol to ConvNeXt-Tiny for \(200\) epochs as a cross-architecture comparison.
\end{itemize}
These runs are treated as physical-extension studies rather than as part of the grouped cross-dataset comparisons in the main text.

\begin{table}[ht]
    \centering
    \caption{Digital FP32 baselines for the evaluated backbones and datasets.}
    \label{tab:supp-fp32-baselines}
    \begin{tabular}{lccc}
        \toprule
        \textbf{Dataset} & \textbf{ResNet-18} & \textbf{ConvNeXt-Tiny} & \textbf{Tiny-ViT-5M} \\
        \midrule
        CIFAR-10 & 95.46\% & 90.74\% & 98.06\% \\
        CIFAR-100 & 78.64\% & 64.12\% & 86.94\% \\
        \bottomrule
    \end{tabular}
\end{table}

\begin{table}[ht]
    \centering
    \caption{Summary of classification accuracy (\%) across architectures and physical regimes. Cross-dataset deployment rows report locked best-checkpoint values; entries with $\pm$ denote stochastic physical-extension evaluations.}
    \label{tab:supp-result-summary}
    \small
    \begin{tabular}{llcc}
        \toprule
        \textbf{Architecture} & \textbf{Regime} & \textbf{CIFAR-10} & \textbf{CIFAR-100} \\
        \midrule
        ResNet-18 & FP32 Digital & 95.46 & 78.64 \\
        ResNet-18 & Standard noisy deployment (R3) & 16.48 & 1.00 \\
        ResNet-18 & Uniform-noise HAT (R4) & 90.37 & 1.00 \\
        Tiny-ViT-5M & FP32 Digital & 98.06 & 86.94 \\
        ConvNeXt-Tiny & FP32 Digital & 90.74 & 64.12 \\
        ConvNeXt-Tiny & Standard noisy deployment (C3) & 70.48 & 23.86 \\
        ConvNeXt-Tiny & Uniform-noise HAT (C4) & 89.91 & 60.54 \\
        ConvNeXt-Tiny & Proportional-noise HAT (3-seed extension) & - & 84.75 $\pm$ 0.72 \\
        \midrule
        Tiny-ViT-5M & Standard noisy deployment (V3) & 89.54 & 44.06 \\
        Tiny-ViT-5M & Uniform-noise HAT (V4) & 91.94 & 65.48 \\
        Tiny-ViT-5M & Front-end compensation (V6) & 82.58 (single-seed) & - \\
        \bottomrule
    \end{tabular}
\end{table}

\subsection{Parameter Provenance}
\label{subsec:parameter-provenance}

This section consolidates parameter provenance and the sensitivity checks used to bound the main claims. Whenever a value is not directly reported in the source literature, we label it explicitly as a proxy estimate or analytical assumption.

\subsubsection{Three-Seed Summary for the Canonical V4 Path}

To document the reproducibility of the canonical Tiny-ViT Ensemble HAT path, we report the protocol-matched three-seed fresh-instance summary directly in the Supplementary Information. Each seed was evaluated on \(10\) fresh D2D instances with \(5\) Monte Carlo passes per instance, and the final cross-seed statistic is computed from the three seed means.

\begin{table}[ht]
\centering
\caption{Three-seed fresh-instance reproducibility summary for the uniform-noise Ensemble HAT regime (V4).}
\label{tab:v4-three-seed-summary}
\begin{tabular}{lcc}
\toprule
\textbf{Seed / Aggregate} & \textbf{Fresh Accuracy} & \textbf{Statistic Type} \\
\midrule
123 & \(86.37\pm1.54\)\% & 10 fresh instances \(\times\) 5 MC \\
456 & \(86.12\pm0.72\)\% & 10 fresh instances \(\times\) 5 MC \\
789 & \(85.99\pm1.94\)\% & 10 fresh instances \(\times\) 5 MC \\
\midrule
Cross-seed aggregate & \(\mathbf{86.16\pm0.19}\)\% & mean \(\pm\) sample std of seed means \\
Pooled fresh instances & \(86.16\pm1.52\)\% & mean \(\pm\) sample std over 30 instance means \\
\bottomrule
\end{tabular}
\end{table}

\FloatBarrier

\begin{table}[ht]
\centering
\caption{Parameter provenance tracking matrix.}
\label{tab:provenance}
\scriptsize
\setlength{\tabcolsep}{1.8pt}
\renewcommand{\arraystretch}{1.1}
\begin{tabular}{>{\raggedright\arraybackslash}p{2.1cm}>{\raggedright\arraybackslash}p{1.9cm}>{\raggedright\arraybackslash}p{2.2cm}>{\raggedright\arraybackslash}p{2.0cm}>{\raggedright\arraybackslash}p{5.0cm}}
\toprule
\textbf{Parameter} & \textbf{Canonical Organic Profile} & \textbf{Zhang 2025 OPECT Case Study} & \textbf{Nonlinear-Write Stress Tests} & \textbf{Provenance / Derivation Notes} \\
\midrule
 Conductance Range (\(G_{\max}/G_{\min}\)) & 10 & 47.3 & 10 & Canonical anchored to scalable OPECTs; Zhang derives from reported max/min current levels \citep{zhang2025opect}. \\ 
 Effective States ($n_{states}$) & 16 (4-bit) & 34 & 16 (4-bit) & Canonical matches general low-precision target. Zhang anchored to Fig.3h \& Supp.Fig.8 \citep{zhang2025opect}. \\ 
 Cycle-to-Cycle Noise (\(\sigma_{\mathrm{C2C}}\)) & 5\% & 2\% & 5\% (Proportional) & Zhang value is a proxy estimate from 8-cycle repeatability (Supp.Fig.15) \citep{zhang2025opect}. Proportional stress scales \(\sigma \propto |G|\). \\
  Device-to-Device Mismatch (\(\sigma_{\mathrm{D2D}}\)) & 10\% & 3\% & 10\% (Proportional) & Zhang uses 3\% as a conservative conductance-domain proxy from the reported $\sim$1\% $V_{th}$ spread \citep{zhang2025opect}. \\
 Retention Decay (\(\tau_1, \tau_2, A_0\)) & 140ms, 610ms, 0.6 & Not fitted & 140ms, 610ms, 0.6 & Canonical dual-exponential fit anchored to Vincze \citep{vincze2025dualplasticity}. Zhang (Fig.2d) is qualitative only. \\ 
 Nonlinear Write Asymmetry (\(NL\)) & 1.0 (LTP), -1.0 (LTD) & Canonical defaults retained & 2.0 (LTP), -2.0 (LTD) & Zhang does not report a pulse-level NL fit, so the case study keeps the canonical symmetric-write defaults rather than injecting a separate literature-fit NL term. Stress tests (\(NL=2\)) enforce severe gradient-scaling asymmetry. \\ 
 Noise Mode & Uniform & Uniform & Proportional & Uniform injects noise normalized to full range. Proportional applies state-dependent noise magnitudes. \\ 
\bottomrule
\end{tabular}
\end{table}

\FloatBarrier

\subsubsection{Vincze et al. 2025 Parameter Extraction}

Because Vincze et al.\ \citep{vincze2025dualplasticity} is currently available as a 2025 Early Access paper, we list the extracted parameters used to define the canonical organic profile explicitly here:

\begin{itemize}
    \item \textbf{Conductance Window}: $G_{\max}/G_{\min} \approx 10$, anchored to the scalable operation regime.
    \item \textbf{Retention Time Constants}: The dual-exponential fit for temporal drift yields a fast decay constant $\tau_1 = 140$~ms, a slow decay constant $\tau_2 = 610$~ms, and an initial amplitude ratio $A_0 = 0.6$. These align with the short-term and long-term plasticity dynamics reported for the device.
    \item \textbf{Nonlinear Write Asymmetry ($NL$)}: While the canonical profile uses symmetric writes ($NL=1.0$), the physical profile derived from the literature indicates moderate write nonlinearity corresponding to $NL_{LTP} = 2.0$ and $NL_{LTD} = -2.0$. In our simulations, $NL=2.0$ maps to a conductance saturation asymmetry (LTP vs. LTD magnitude at the window midpoint) of approximately 3:1, a regime consistent with reported long-pulse dynamics in DNTT-based organic synaptic transistors \citep{vincze2025dualplasticity}. We evaluate this directly in our severe stress tests.
\end{itemize}

\subsubsection{Proxy Estimate Sensitivity Analysis}

The Zhang 2025 OPECT values for \(\sigma_{\mathrm{C2C}}\) and \(\sigma_{\mathrm{D2D}}\) are transparent proxy estimates rather than directly measured full-array noise distributions. We therefore ran a compact \(4 \times 5\) sensitivity sweep on the Ensemble HAT model, spanning \(\sigma_{\mathrm{C2C}} \in \{1, 2, 5, 8\}\%\) and \(\sigma_{\mathrm{D2D}} \in \{2, 3, 5, 10, 15\}\%\) while keeping the remaining case-study settings fixed. Figure~\ref{fig:supp-contour-map} shows that the conclusion is insensitive to the exact proxy choice: performance changes primarily along the D2D axis, whereas the tested C2C range remains effectively masked by the digital recovery path.

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.72\textwidth]{fig_proxy_sensitivity_map}
    \caption{\textbf{Zhang-proxy C2C/D2D sensitivity sweep.} Cell annotations report the mean accuracy over \(10\) Monte Carlo evaluations at each operating point. The nominal proxy point \((\sigma_{\mathrm{C2C}}=2\%, \sigma_{\mathrm{D2D}}=3\%)\) is outlined. Across the tested range, accuracy changes are almost entirely driven by D2D mismatch, while the C2C axis remains flat to within the Monte Carlo uncertainty.}
    \label{fig:supp-contour-map}
\end{figure}

\begin{table}[ht]
\centering
\caption{Ensemble HAT accuracy under a Zhang-proxy C2C/D2D sensitivity sweep.}
\label{tab:sensitivity}
\small
\begin{tabular}{lccccc}
\toprule
\textbf{C2C \textbackslash D2D} & \textbf{2\%} & \textbf{3\% (Nominal)} & \textbf{5\%} & \textbf{10\%} & \textbf{15\%} \\
\midrule
\textbf{1\%} & 88.57\% & 88.53\% & 88.33\% & 87.30\% & 84.59\% \\
\textbf{2\% (Nominal)} & 88.57\% & 88.53\% & 88.33\% & 87.30\% & 84.59\% \\
\textbf{5\%} & 88.57\% & 88.53\% & 88.33\% & 87.30\% & 84.59\% \\
\textbf{8\%} & 88.57\% & 88.53\% & 88.33\% & 87.30\% & 84.59\% \\
\bottomrule
\end{tabular}
\end{table}

95\% confidence intervals are sub-0.1~pp for all nominal points.

\begin{table}[ht]
\centering
\caption{Hook-based ADC non-ideality check for the canonical Tiny-ViT V4 deployment. Offset, gain, and INL are injected at calibrated analog-layer outputs during full-test inference on CIFAR-10.}
\label{tab:adc-nonideality}
\begin{tabular}{lccc}
\toprule
\textbf{Configuration} & \textbf{Mean Acc. (\%)} & \textbf{Std. (\%)} & \textbf{\(\Delta\) vs. ideal (pp)} \\
\midrule
Ideal & 82.04 & 0.16 & +0.00 \\
Offset \(\pm 0.5\) LSB & 82.07 & 0.21 & +0.03 \\
Gain \(\pm 5\%\) & 81.87 & 0.30 & -0.17 \\
INL \(0.5\) LSB & 80.85 & 0.12 & -1.19 \\
Combined realistic & 81.86 & 0.28 & -0.18 \\
Combined pessimistic & 76.90 & 0.27 & -5.14 \\
\bottomrule
\end{tabular}
\end{table}

Moderate ADC non-idealities change accuracy by $<$0.2~pp, whereas the pessimistic combination degrades by $5.14$~pp, confirming that peripheral precision is gate-keeping only once errors exceed the moderate range.

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.74\textwidth]{fig_sobol_sensitivity}
    \caption{\textbf{First-order Sobol sensitivity indices derived from the contour sweep.} The left group reports indices over the full \(7 \times 9\) D2D--ADC grid, where ADC resolution dominates the variance budget. The right group restricts the analysis to the deployment-relevant operating region (\(\mathrm{ADC}\geq 6\) bits, \(\sigma_{\mathrm{D2D}}\leq 15\%\)), where the residual variance is driven primarily by D2D mismatch after readout precision has saturated.}
    \label{fig:supp-sobol}
\end{figure}

\FloatBarrier

\subsubsection{Uniform vs. State-Dependent Retention Comparison}

We compared uniform retention with a state-dependent model where high-conductance states decay 20\% faster. The retained Ensemble-HAT CIFAR-10 model serves as the test case.

\begin{table}[ht]
\centering
\caption{Retention accuracy comparison: uniform vs. state-dependent decay.}
\label{tab:retention-comparison}
\begin{tabular}{lccc}
\toprule
\textbf{Time (s)} & \textbf{Uniform (\%)} & \textbf{State-Dep (\%)} & \textbf{Diff (pp)} \\
\midrule
0    & 90.77 & 90.80 & +0.03 \\
1    & 90.07 & 90.08 & +0.01 \\
10   & 89.78 & 89.81 & +0.03 \\
100  & 89.87 & 89.89 & +0.02 \\
1000 & 89.82 & 89.90 & +0.08 \\
\bottomrule
\end{tabular}
\end{table}

The accuracy difference stays below 0.1 percentage points. This validates the uniform retention assumption for Ensemble-HAT CIFAR-10; it does not claim state dependence is negligible in general.

\FloatBarrier

\begin{table}[ht]
\centering
\caption{Classification accuracy (\%) on Flowers-102 across physical regimes. The asterisk marks a single-run ConvNeXt boundary estimate and should not be interpreted as a stable cross-architecture baseline.}
\label{tab:supp-flowers-results}
\begin{tabular}{llc}
\toprule
\textbf{Architecture} & \textbf{Regime} & \textbf{Flowers-102} \\
\midrule
Tiny-ViT-5M & FP32 Digital & 97.97 \\
ConvNeXt-Tiny & FP32 Digital & 33.22* \\
Tiny-ViT-5M & V3 (Canonical Noise) & 4.81 ± 1.25 \\
Tiny-ViT-5M & V4 (Canonical HAT) & 22.48 ± 1.82 \\
\bottomrule
\end{tabular}
\end{table}

Because the ConvNeXt Flowers-102 baseline is available only as a single-run estimate, we use this dataset primarily as a low-data stress test rather than as a definitive cross-architecture comparison point.

\FloatBarrier

\subsection{Supplementary Methods}
\label{subsec:supp-methods}

\subsubsection{Differential Conductance Mapping Details}

Each analog-mapped weight tensor is converted into a differential-pair conductance representation over a bounded physical range $[G_{\min}, G_{\max}]$. The positive and negative branches are quantized to $n_{\mathrm{states}}$ levels with a straight-through estimator (STE), and the effective analog weight is recovered from the differential conductance. The layer-wise digital scale recovery is
\[
\hat{W}_{ij}=s_{\ell}G_{\mathrm{eff},ij},
\qquad
s_{\ell}=\frac{w_{\max}}{\Delta G},
\qquad
\Delta G = G_{\max}-G_{\min},
\]
with the optional retention-time recalibration
\[
s_{\ell}^{\mathrm{ret}}=\frac{w_{\max}}{\max_{i,j}|G_{\mathrm{eff},ij}|+\epsilon}.
\]
The recovered weight step is therefore
\[
\Delta_{\hat{W}} = s_{\ell}\frac{\Delta G}{n_{\mathrm{states}}-1},
\]
so the scale-masking regime discussed in the main text corresponds to perturbations satisfying $|s_{\ell}\delta G_{\mathrm{eff},ij}| < \Delta_{\hat{W}}/2$. We model this scale recovery as ideal calibrated digital post-processing rather than as an explicit physical peripheral circuit. Deterministic positive/negative branch asymmetry is not separately parameterized beyond the sampled D2D/C2C perturbations; any perfect common-mode cancellation implied by the differential form is a simplifying layout-level assumption.

\subsubsection{Variability and Write Dynamics Modeling}

Device-to-device (D2D) mismatch is modeled as a fixed Gaussian perturbation sampled once at initialization,
\[
\delta g_{\mathrm{D2D}} \sim \mathcal{N}\!\left(0,\, \sigma_{\mathrm{D2D}}\Delta G\right),
\qquad
\Delta G = G_{\max}-G_{\min},
\]
whereas cycle-to-cycle (C2C) variation is resampled on each forward pass as
\[
\delta g_{\mathrm{C2C}} \sim \mathcal{N}\!\left(0,\, \sigma_{\mathrm{C2C}}\Delta G\right).
\]
In proportional mode, the same Gaussian law is used but the perturbation magnitude scales with the instantaneous conductance state, i.e.\ $\delta g \propto |G|$ rather than $\Delta G$.

Nonlinear write modeling utilizes \texttt{NL\_LTP} and \texttt{NL\_LTD} coefficients to scale the STE backward pass. The surrogate follows
\[
\frac{\partial L}{\partial G}
=
\begin{cases}
\dfrac{\partial L}{\partial \tilde{G}}
\left(\dfrac{G_{\max}-G}{G_{\max}-G_{\min}}\right)^{NL_{\mathrm{LTP}}-1},
& \dfrac{\partial L}{\partial \tilde{G}} \ge 0 \\[10pt]
\dfrac{\partial L}{\partial \tilde{G}}
\left(\dfrac{G-G_{\min}}{G_{\max}-G_{\min}}\right)^{|NL_{\mathrm{LTD}}|-1},
& \dfrac{\partial L}{\partial \tilde{G}} < 0
\end{cases}
\]
so $NL_{\mathrm{LTP}}=1$ and $|NL_{\mathrm{LTD}}|=1$ recover the identity STE. Retention is implemented as a decay factor applied to the programmed conductance above the floor $G_{\min}$:
\[
G(t)=G_{\min}+\left(G_{\mathrm{prog}}-G_{\min}\right)\,f_{\mathrm{ret}}(t),
\]
with
\[
f_{\mathrm{ret}}(t)=A_0 + A_1 e^{-t/\tau_1} + A_2 e^{-t/\tau_2},
\qquad
A_1=A_2=\frac{1-A_0}{2},
\]
and parameters anchored to measured DNTT transients \citep{vincze2025dualplasticity}. In the optional state-dependent extension, the same form is used with conductance-dependent effective time constants so that high-conductance states decay modestly faster.

\subsubsection{Energy Profiler Implementation}

The analytical energy model estimates cost across four categories:
\begin{enumerate}
    \item \textbf{Analog MAC}: $E_{\text{MAC}} = N_{\text{ops}} \cdot E_{\text{cell}}$, which sets $E_{\text{cell}}=100$~fJ per differential-pair MAC in the default profiler.
    \item \textbf{Data Conversion}: ADC energy is charged per output element and DAC energy per input element, with behavioral DNL/INL modeling. The default implementation uses $E_{\text{ADC},8b}=25$~fJ, $E_{\text{DAC},8b}=30$~fJ, and $t_{\text{ADC},8b}=100$~ns for a SAR-like 8-bit readout proxy.
    \item \textbf{Digital Operations}: Fixed-point costs for Softmax, LayerNorm, and scaling.
    \item \textbf{Memory Traffic}: SRAM read/write energy for weight programming and intermediate activation buffering.
\end{enumerate}
The reported total is accumulated as
\[
E_{\mathrm{total}}
=
E_{\mathrm{MAC}}
+
E_{\mathrm{ADC}}
+
E_{\mathrm{DAC}}
+
E_{\mathrm{digital}}
+
E_{\mathrm{buffer}},
\]
with
\[
E_{\mathrm{ADC}} = N_{\mathrm{ADC}}E_{\mathrm{ADC},8b},
\qquad
E_{\mathrm{DAC}} = N_{\mathrm{DAC}}E_{\mathrm{DAC},8b},
\]
and the digital/buffer terms evaluated from operator and access counts. These constants are literature-proxy placeholders for a first-order edge-node model, not measured circuit values, and any reported energy ratios are contingent on these assumptions. Interconnect overhead is behaviorally absorbed into the memory-access terms.

\subsection{Supplementary Figures}
\label{subsec:supplementary-figures}

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.96\textwidth]{fig9_noise_sensitivity}
    \caption{\textbf{Continuous noise sensitivity and ADC sweep for the Tiny-ViT V4 checkpoint.} \textbf{Left:} mean accuracy heatmap under a continuous joint sweep of \(\sigma_{\mathrm{C2C}}\) and \(\sigma_{\mathrm{D2D}}\) in the uniform-noise regime. \textbf{Right:} ADC-bit sweep for the same checkpoint, highlighting the 6-bit knee and the saturation that follows. Only the model with available continuous-sweep data is shown; no placeholder panel is inserted for missing architectures.}
    \label{fig:supp-noise-sensitivity}
\end{figure}

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.96\textwidth]{fig_fresh_instance_ablation}
    \caption{\textbf{Fresh-instance robustness and D2D-resampling ablation.} \textbf{Left:} evaluation on \(10\) unseen fixed D2D realizations for the original plotted checkpoint. Standard HAT collapses to chance on every fresh array (10.00\%), whereas Ensemble HAT preserves \(86.37\pm1.54\%\) accuracy across the same deployment set; a protocol-matched three-seed rerun gives \(86.16\pm0.19\%\) across seed-level fresh-instance means. \textbf{Right:} exploratory held-out-accuracy scan under different D2D-resampling cadences during training. In this 50-epoch scan, epoch-level resampling reaches 88.41\%, exceeding fixed-mask training, slower refresh schedules, and the corresponding short-run per-batch perturbation control. The panel is intended to show that cadence matters, rather than to serve as the final paper-locked estimate for every schedule.}
    \label{fig:supp-zero-shot-transfer}
\end{figure}

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.985\textwidth]{fig_attention_maps}
    \caption{\textbf{Attention heatmaps for three representative CIFAR-10 samples across the main deployment regimes.} Columns show cat, truck, and automobile. Rows, from top to bottom, show the original input image, the V1 digital baseline, the V3 fixed-mask noisy deployment, the V4 hardware-aware trained deployment, and the V6 front-end-compensated deployment. Color intensity denotes normalized head-averaged attention response.}
    \label{fig:supp-attention-maps}
\end{figure}

To complement the qualitative maps in Supplementary Fig.~\ref{fig:supp-attention-maps}, we also tracked the mean head-averaged attention entropy for the three representative samples shown there. Averaged across the samples, the entropy rises from \textbf{3.38} in the digital baseline to \textbf{3.61} under standard noisy deployment, indicating a broader and less selective spatial focus. Under HAT, the entropy drops to \textbf{3.07}, and the model recovers the correct class label on all three displayed examples, whereas the fixed-mask noisy model is correct on only one. These examples provide a compact quantitative complement to the attention maps by showing that HAT restores a sharper task-aligned focus pattern.

\subsection{Extended Results: Retention and Frontend Distortion}
\label{subsec:extended-results}

\subsubsection{Retention and Temporal Drift Details}

Supplementary Figure~\ref{fig:supp-retention-curve} shows the corrected V4 retention decay with dynamic scale recalibration and co-decay of the retained D2D buffers. The curve exhibits a two-phase behavior: a rapid early drop from 91.63\% to 82.66\% at 1 s, followed by a broad plateau near \textbf{79\% accuracy} from 10 s to $10{,}000$ s (79.13\%, 79.05\%, 79.35\%, and 79.51\% at 10, 100, 1000, and $10{,}000$ s, respectively). Under the present first-order retention model, this indicates that long-horizon analog inference remains partially viable once gain recalibration is included in the inference stack. The retention results reported here were obtained under the same uniform double-exponential decay model described in the main-text methodology section. The codebase now additionally supports true state-dependent retention where high-conductance states decay faster, but the canonical V4 results in this paper use the uniform model. A direct comparison between these two models (detailed in Section~\ref{subsec:parameter-provenance}) shows that the accuracy difference remains below 0.1 percentage points across all tested time intervals, validating the sufficiency of the uniform model for the present parameter regime.

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.92\textwidth]{fig7_retention_curve}
    \caption{\textbf{Retention decay under programmed weight drift.} Accuracy is plotted against time since programming for ConvNeXt C9 and Tiny-ViT V4 under dynamic scale recalibration with co-decay of the retained D2D buffers. Both models exhibit a rapid early drop followed by a broad plateau, indicating partial long-horizon viability under the present uniform double-exponential retention model.}
    \label{fig:supp-retention-curve}
\end{figure}

\subsubsection{Theoretical Analysis of Frontend Compensation}
\label{note:frontend-theory}

\paragraph{Distinction from ISP gamma correction.}
The inverse-gamma preprocessor $P_{\text{in}} = X^{1/\gamma_{\text{phys}}}$ is superficially similar to gamma correction in digital image processing (ISP), but the two serve different physical purposes and operate in different noise regimes. In ISP, gamma encoding ($\gamma_{\text{ISP}} \approx 2.2$) compresses high-intensity pixel values to match the nonlinear response of the human visual system, while its inverse (gamma decoding) expands the signal before display. The goal is perceptual uniformity, not physical linearization, and the noise model is typically signal-independent or post-quantization. The transformation is applied to already-digitized pixels.

In the organic phototransistor front end modeled here, the nonlinearity is a physical transduction property: photocurrent follows $I_{\text{photo}} \propto P^{\gamma_{\text{phys}}}$ with $\gamma_{\text{phys}}$ typically between 0.5 and 2.0. The inverse-gamma preprocessor $P_{\text{in}} = X^{1/\gamma_{\text{phys}}}$ is applied \emph{before} the physical transduction step, not after sensor readout. Consequently, shot noise $\varepsilon_{\text{shot}}$ is injected \emph{after} the compensation and scales with the compensated photocurrent. This ordering is critical: ISP gamma decoding operates on already-noisy quantized pixels, whereas the front-end compensation reshapes the signal that subsequently acquires noise. The result is a regime-dependent trade-off that has no analogue in ISP: for $\gamma_{\text{phys}} > 1$, compensation linearizes the signal and improves accuracy; for $\gamma_{\text{phys}} < 1$, it amplifies shot-noise variance across the entire intensity range, yielding marginal or negative benefit (Table~\ref{tab:supp-frontend-gamma-scan}). This signal-to-noise coupling is intrinsic to the phototransistor transduction chain and is captured explicitly by the compensated photocurrent model introduced in the main text. Consequently, the compensation exponent is chosen to linearize a physical transduction law, not to match a perceptual response curve.

Because the preprocessor is parameterized by $\gamma_{\text{phys}}$ and exposed through the same JSON device profile as conductance window, retention, and variability, it functions as a \emph{replaceable system-level design primitive} rather than as a fixed preprocessing step.

\paragraph{Why the transformer amplifies front-end distortion.}
The difference in front-end sensitivity between ViT and CNN can be understood from the way each architecture processes perturbed inputs. In a CNN, the initial convolutional layers apply local receptive-field operations. A sublinear photoresponse ($\gamma_{\text{phys}} > 1$) compresses bright-region contrast, but because each output pixel depends only on a small spatial neighborhood, the distortion remains localized in early feature maps. Subsequent pooling and strided convolutions further average these local perturbations, and the piecewise-linear ReLU activations limit the propagation of intensity-dependent scaling errors. The net effect is that CNNs tolerate moderate input nonlinearities because spatial averaging and local nonlinearity act as implicit regularizers.

In a ViT, by contrast, the input is partitioned into patches and linearly projected into token embeddings. A sublinear photoresponse alters the relative intensities \emph{within} each patch before the embedding projection sees the data. The critical difference arises in self-attention. The attention map is computed as $A = \text{softmax}(QK^{\top}/\sqrt{d_k})$, where $Q$ and $K$ are linear projections of the token embeddings. If the photoresponse nonlinearity changes the intensity distribution within a patch, the resulting token embedding shifts, and the dot-product similarity changes in a \emph{nonlinear} way because the embedding was learned on clean data. The softmax then \emph{exponentiates} these similarity changes: a small perturbation in one token can redistribute attention weights across the entire image. Because attention is global, there is no spatial averaging to dampen the perturbation before classification.

The experimental evidence supports this mechanism. Under the raw photoresponse ($\gamma_{\text{phys}}=2.0$ without compensation), ResNet-18 (R4) degrades from 90.37\% to 84.04\% ($-$6.3 pp). The transformer architecture is structurally more exposed to front-end distortion because global attention lacks the spatial averaging present in CNNs; this mechanism-level expectation is consistent with the ResNet-18 drop and with the stronger compensation sensitivity observed along the Tiny-ViT V6 pathway. After inverse-gamma compensation, both architectures recover, but the ViT recovery remains more sensitive to the compensation exponent because residual noise variance also propagates through the global attention map.

\subsubsection{Physical Frontend Compensation Details}

We evaluated the impact of the organic phototransistor's sub-linear response ($\gamma_{\mathrm{phys}}$) and dark current ($I_{\text{dark}}$) on system-level accuracy. Supplementary Figure~\ref{fig:supp-frontend-compensation} summarizes the ResNet-18 (R4) and Tiny-ViT (V6) results under various front-end configurations. The inverse-gamma compensation mechanism successfully restores accuracy for high $\gamma_{\mathrm{phys}}$ (e.g., $\gamma_{\mathrm{phys}}=2.0$), providing a gain of up to +5.8 pp compared to the raw physical response. However, we observe that for $\gamma_{\mathrm{phys}} < 1$, the compensation is less effective due to shot-noise amplification. Our analytical signal-to-noise ratio (SNR) analysis (Supplementary Figure~\ref{fig:supp-snr-curves}) confirms that while inverse-gamma preprocessing linearizes the signal, it amplifies noise variance across the entire intensity range when $1/\gamma_{\mathrm{phys}} > 1$.

Table~\ref{tab:supp-frontend-gamma-scan} presents the full ResNet-18 HAT accuracy matrix across the tested $\gamma_{\mathrm{phys}}$ and $I_{\text{dark}}$ values. The pattern is consistent: compensation gains are largest when the raw photoresponse is most nonlinear ($\gamma_{\mathrm{phys}}=2.0$), shrink toward unity as $\gamma_{\mathrm{phys}} \rightarrow 1$, and turn flat when $\gamma_{\mathrm{phys}} < 1$ because shot-noise amplification outweighs the modest signal benefit.

\begin{table}[ht]
    \centering
    \caption{\textbf{ResNet-18 HAT accuracy (\%) under inverse-gamma compensation.} Each cell reports compensated / raw accuracy and the gain in percentage points.}
    \label{tab:supp-frontend-gamma-scan}
    \small
    \setlength{\tabcolsep}{4pt}
    \begin{tabular}{c|cccc}
        \toprule
        $\gamma_{\mathrm{phys}}$ & 10~pA & 100~pA & 1~nA & 10~nA \\
        \midrule
        0.5 & 89.79 / 87.71 (+2.08) & 89.78 / 87.60 (+2.18) & 89.17 / 87.69 (+1.48) & 89.21 / 87.87 (+1.34) \\
        0.7 & 89.51 / 88.90 (+0.61) & 89.72 / 89.24 (+0.48) & 89.92 / 89.11 (+0.81) & 89.68 / 89.17 (+0.51) \\
        1.0 & 89.84 / 89.61 (+0.23) & 89.61 / 89.46 (+0.15) & 90.00 / 89.70 (+0.30) & 89.71 / 89.67 (+0.04) \\
        1.5 & 89.56 / 88.28 (+1.28) & 89.57 / 88.06 (+1.51) & 89.89 / 87.58 (+2.31) & 89.64 / 88.21 (+1.43) \\
        2.0 & 89.85 / 84.04 (+5.81) & 89.61 / 84.29 (+5.32) & 89.99 / 84.47 (+5.52) & 89.87 / 84.22 (+5.65) \\
        \bottomrule
    \end{tabular}
\end{table}

\textbf{Core Value Conclusion}: Inverse-gamma compensation provides a physical-level regularization effect in low-light/dark scenarios; however, in high-intensity regions, noise variance increases with the compensation exponent, constituting a fundamental \textbf{design trade-off}. Consequently, the front-end benefit is most pronounced in high-dark-current regimes where signal recovery outweighs noise amplification.

\subsubsection{Optimal Compensation Exponent}
\label{note:optimal-gamma}

The physical inverse $\gamma_c = 1/\gamma_{\text{phys}}$ exactly linearizes the photoresponse in the noiseless limit, but it is not necessarily optimal when shot noise is present. We derive the mean-squared-error-optimal compensation exponent for a uniform input distribution.

The effective signal deviation from the ideal linear response $I_{\text{ideal}} = \alpha X + I_{\text{dark}}$ is
\begin{equation}
    \Delta(X; \gamma_c) = \alpha \left(X^{\gamma_c \gamma_{\text{phys}}} - X\right) + \varepsilon_{\text{shot}},
\end{equation}
with $\text{Var}[\varepsilon_{\text{shot}}] = \sigma^2 (\alpha X^{\gamma_c \gamma_{\text{phys}}} + I_{\text{dark}})$. The expected MSE is
\begin{equation}
    \mathcal{L}(\gamma_c) = \alpha^2 \int_0^1 \!\left(X^{\gamma_c \gamma_{\text{phys}}} - X\right)^2 dX + \sigma^2 \int_0^1 \!\left(\alpha X^{\gamma_c \gamma_{\text{phys}}} + I_{\text{dark}}\right) dX.
\end{equation}

Setting $\beta = \gamma_c \gamma_{\text{phys}}$ and evaluating the integrals gives
\begin{equation}
    \mathcal{L}(\beta) = \alpha^2\!\left[\frac{1}{2\beta+1} - \frac{2}{\beta+2} + \frac{1}{3}\right] + \sigma^2\!\left[\frac{\alpha}{\beta+1} + I_{\text{dark}}\right].
\end{equation}

Differentiating at $\beta = 1$ (the physical inverse):
\begin{equation}
    \left.\frac{d\mathcal{L}}{d\beta}\right|_{\beta=1} = -\frac{\sigma^2 \alpha}{4} < 0 \quad (\text{for } I_{\text{dark}} \approx 0).
\end{equation}

Because the derivative is negative, the MSE can be reduced by increasing $\beta$; i.e., the optimal compensation exponent is \textbf{milder} than the physical inverse when shot noise is present. The deviation is small for typical device parameters ($\sigma^2 \ll \alpha$), which explains why the physical inverse remains an excellent practical approximation. We note that this derivation minimizes pixel-level mean-squared error rather than task-level classification loss; the learnable-compensation experiment (E3) therefore tests whether end-to-end accuracy optimization recovers a deviation consistent with the MSE prediction.

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.92\textwidth]{fig6_physical_compensation}
    \caption{\textbf{Impact of front-end nonlinearity and dark current.} Inverse-gamma compensation helps in dark regimes but amplifies noise elsewhere.}
    \label{fig:supp-frontend-compensation}
\end{figure}

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.85\textwidth]{fig3_snr_curves}
    \caption{\textbf{Analytical SNR trends under inverse-gamma compensation derived from the front-end response model used in the simulator.}}
    \label{fig:supp-snr-curves}
\end{figure}

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.88\textwidth]{fig8_pareto_energy_accuracy}
    \caption{\textbf{Energy--accuracy placement of the digital baseline and hybrid V2--V7 family.} Energy values are computed with the placeholder first-order analytical model (Supplementary Methods) and are intended for relative comparison only.}
    \label{fig:supp-pareto}
\end{figure}

\subsubsection{Retention Sensitivity Analysis}
\label{subsec:retention-sensitivity}
To assess the robustness of the retention drift predictions against uncertainties in the literature-derived proxy parameters, we conducted a sensitivity sweep over the persistent fraction ($A_0$) and the inference time $t$. As summarized in Table~\ref{tab:retention-sensitivity}, the system conclusions remain qualitatively unchanged within a $\pm 50\%$ variance of retention proxy parameters. Even when the conductance persistent fraction is reduced to 40\% ($A_0 = 0.4$), the model retains $\sim$87.6\% accuracy up to $10{,}000$ s, confirming that the identified retention trends are robust to local variations in the proxy decay constants.

\begin{table}[ht]
\centering
\caption{\textbf{Retention sensitivity to the persistent fraction $A_0$ across time scales.}}
\label{tab:retention-sensitivity}
\begin{tabular}{@{}lcccc@{}}
\toprule
\textbf{Persistent Fraction ($A_0$)} & \textbf{1 s} & \textbf{100 s} & \textbf{1,000 s} & \textbf{10,000 s} \\
\midrule
$0.8$ & 90.55\% & 90.49\% & 90.49\% & 90.49\% \\
$0.6$ (Canonical) & -- & 89.88\% & 89.88\% & 89.88\% \\
$0.4$ & 88.62\% & 87.61\% & 87.61\% & 87.61\% \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Differential-Pair Asymmetry Sensitivity}
\label{subsec:supp-asymmetry-sensitivity}

Systematic asymmetry between the positive and negative branches of differential weight mapping is evaluated by injecting controlled mismatch into the canonical uniform-noise HAT model. Consistent with the implementation, the branch update includes an offset correction so that the imposed asymmetry does not add a trivial constant differential bias from the shared conductance floor:
\begin{equation}
G^{(\alpha)}_{\mathrm{eff}} = (1+\alpha)G^{+} - (1-\alpha)G^{-} - 2\alpha G_{\min}
\end{equation}
where $\alpha \in [0, 0.10]$ is the asymmetry factor. Table~\ref{tab:asymmetry-sensitivity} summarizes the accuracy degradation.

\begin{table}[ht]
\centering
\caption{\textbf{Differential-pair asymmetry sensitivity.} Accuracy degradation under systematic branch mismatch for Tiny-ViT V4 (CIFAR-10).}
\label{tab:asymmetry-sensitivity}
\begin{tabular}{@{}lccc@{}}
\toprule
\textbf{Asymmetry $\alpha$} & \textbf{Mean Accuracy} & \textbf{Std Dev} & \textbf{Degradation} \\
\midrule
0.0 (baseline) & 91.78\% & 0.10\% & — \\
0.01 (1\%) & 90.29\% & 0.15\% & 1.49\% \\
0.02 (2\%) & 85.73\% & 0.59\% & 6.05\% \\
0.05 (5\%) & 33.81\% & 1.76\% & 57.97\% \\
0.10 (10\%) & 10.00\% & 0.00\% & 81.78\% \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.85\textwidth]{figS1_asymmetry_concept}
    \caption{\textbf{Differential pair asymmetry concept.} 
    Systematic mismatch between positive and negative branches 
    (asymmetry factor $\alpha$) degrades the effective differential signal.
    Quantitative sensitivity analysis shows tolerance up to 1\% asymmetry 
    with $<$2\% accuracy degradation, but nonlinear collapse beyond 2\%.}
    \label{fig:supp-asymmetry-concept}
\end{figure}

The results indicate that differential-pair symmetry is a critical layout constraint: asymmetry up to 1\% is well-tolerated, but performance degrades nonlinearly beyond 2\%, with complete failure at 10\%. This motivates the need for explicit mismatch calibration or resistance balancing circuits in fabricated organic CIM arrays.

\subsection{Physical Non-Ideality Sensitivity}
\label{subsec:supp-nonideality-sensitivity}

Position-dependent IR drop and sneak-path currents are evaluated by injecting controlled non-idealities into the canonical uniform-noise HAT model. The two perturbations are implemented independently at the conductance level:
\begin{align}
G^{(\mathrm{IR})} &= G \left(1 - \delta_{\mathrm{IR}}\right),
& \delta_{\mathrm{IR}} &\sim \mathcal{U}(0, \delta_{\mathrm{IR}}^{\max}) \\
G^{(\mathrm{sneak})} &= G + \epsilon_{\mathrm{sneak}},
& \epsilon_{\mathrm{sneak}} &\sim \mathcal{N}\!\left(0, \sigma_{\mathrm{sneak}} G_{\max}\right)
\end{align}
where $\delta_{\mathrm{IR}}^{\max} \in [0, 0.03]$ and $\sigma_{\mathrm{sneak}} \in [0, 0.02]$ are the sweep controls based on ReRAM literature estimates. Table~\ref{tab:nonideality-sensitivity} summarizes the accuracy degradation.

\begin{table}[ht]
\centering
\caption{\textbf{Physical non-ideality sensitivity.} Accuracy degradation under IR drop and sneak-path effects for Tiny-ViT V4 (CIFAR-10).}
\label{tab:nonideality-sensitivity}
\small
\begin{tabular}{@{}lccc@{}}
\toprule
\textbf{IR Drop} & \textbf{Sneak Path} & \textbf{Mean Accuracy} & \textbf{Degradation} \\
\midrule
0.0\% & 0.0\% & 91.86\% & — \\
0.0\% & 1.0\% & 90.90\% & 0.96\% \\
0.0\% & 2.0\% & 89.36\% & 2.50\% \\
1.0\% & 0.0\% & 91.45\% & 0.41\% \\
1.0\% & 1.0\% & 91.31\% & 0.55\% \\
1.0\% & 2.0\% & 90.03\% & 1.83\% \\
2.0\% & 0.0\% & 91.29\% & 0.57\% \\
2.0\% & 1.0\% & 91.21\% & 0.65\% \\
2.0\% & 2.0\% & 89.88\% & 1.98\% \\
3.0\% & 0.0\% & 91.12\% & 0.74\% \\
3.0\% & 1.0\% & 91.11\% & 0.75\% \\
3.0\% & 2.0\% & 89.70\% & 2.16\% \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.95\textwidth]{figS2_nonideality}
    \caption{\textbf{Physical non-idealities in resistive arrays.} 
    (Left) IR drop causes position-dependent voltage loss along wordlines. 
    (Right) Sneak paths create unintended current leakage through adjacent cells.
    Quantitative sensitivity analysis (Table~\ref{tab:nonideality-sensitivity}) 
    indicates these non-idealities degrade accuracy by $<$2.2\% for typical 
    1--3\% effect magnitudes, suggesting the qualitative advantage is robust 
    to moderate array-level effects.}
    \label{fig:supp-nonideality}
\end{figure}

The results demonstrate that the hybrid-analog architecture maintains reasonable accuracy even under combined non-idealities. The worst-case degradation (2.16\% at 3\% IR + 2\% sneak) remains limited within the tested lower-bound probe range and therefore provides a sensitivity check consistent with the use of first-order behavioral models for early-stage design space exploration, rather than a full parasitic-validation result for scaled organic arrays.

\subsection{Comparison with Inorganic RRAM Baselines}
\label{subsec:inorganic-comparison}

To contextualize the specialized framework for organic optoelectronic devices, we contrast our methodology with mature inorganic simulations (e.g., RRAM arrays modeled via NeuroSim, CrossSim, or AIHWKIT) \citep{peng2020dnnneurosim,crosssim2024,rasch2021aihwkit}. Inorganic devices typically exhibit faster write speeds, larger on/off ratios, and primarily electrical programming dynamics. In contrast, the organic devices modeled here offer multi-modal (optoelectronic) tuning and excellent low-power analog behavior, but often suffer from slower temporal drift, pronounced state-dependent retention, and complex nonlinear write asymmetry.

As a methodological consistency check in a shared regime, we evaluated an existing ResNet-18/CIFAR-10 model over the full 10k-sample test set under matched 4-bit/noise assumptions: the digital baseline reached \(95.46\%\), while an AIHWKIT analog-tile baseline reached \(90.08\pm0.21\%\) over 10 runs. We use this result only to verify numerical consistency between the present simulator and a mature inorganic toolkit under one matched regime; it does not establish physical validation for retention, photoresponse, or organic nonlinear writing. Thus, rather than claiming to outperform mature RRAM simulators on digital benchmarks, our framework serves as a complementary tool tailored to expose the system-level consequences of organic-specific physical non-idealities, such as shot noise amplification from inverse-gamma front ends.

The subset-sampling protocol for this throughput-constrained CrossSim comparison
is given later in Supplementary Note~S1, together with the confidence-interval
estimate.

\subsection{Automated Profile Fitting Pipeline}

For later measured-device studies, we also provide an automated fitting utility that ingests four raw-data categories:
\begin{enumerate}
    \item \textbf{Pulsed Programming Curves}: Fits the conductance range ($G_{\min}, G_{\max}$) and nonlinearity coefficients ($NL_{LTP}, NL_{LTD}$) using a behavioral exponential model.
    \item \textbf{Cycle-to-Cycle (C2C) Statistics}: Extracts the $\sigma_{\mathrm{C2C}}$ noise parameter from repeated write-verify measurements at identical target states.
    \item \textbf{Device-to-Device (D2D) Mismatch}: Calculates the $\sigma_{\mathrm{D2D}}$ parameter from conductance distributions across multiple physical devices in an array.
    \item \textbf{Retention Decay}: Fits the double-exponential decay constants ($\tau_1, \tau_2, A_0$) from long-term conductance-vs-time measurements.
\end{enumerate}

The output is a standardized device-profile specification that can be used directly in the inference and HAT workflow. In the present manuscript, this fitter is included as supporting infrastructure for future measured-device calibration rather than as a core source of the reported headline results.

\subsection{Parameter Risk Matrix \& Sensitivity Analysis}
\label{subsec:parameter-risk-matrix}

To contextualize the simulation-only behavioral methodology of the present study, Table~\ref{tab:parameter-risk} provides a risk assessment of the key proxy parameters used in the modeling framework. By transparently disclosing the range of physical uncertainty and its consequent impact on system-level conclusions, we establish the robustness boundaries of the reported results.

\begin{table}[ht]
\centering
\caption{\textbf{Core Parameter Risk and Robustness Assessment.}}
\label{tab:parameter-risk}
\resizebox{\textwidth}{!}{
\begin{tabular}{@{}ll>{\raggedright\arraybackslash}p{3cm}>{\raggedright\arraybackslash}p{6cm}>{\raggedright\arraybackslash}p{5cm}@{}}
\toprule
\textbf{Parameter} & \textbf{Nominal Value} & \textbf{Uncertainty Range} & \textbf{Impact on Conclusions} & \textbf{Framework Check} \\
\midrule
D2D Variability ($\sigma_{\mathrm{D2D}}$) & 3.0\% (Zhang 2025) & 1.0\% -- 15.0\% & \textbf{High.} Directly drives the need for Ensemble HAT. If $\sigma_{\mathrm{D2D}} < 1\%$, the relative value of HAT diminishes. & Table~\ref{tab:sensitivity} demonstrates robustness under a 1\%--15\% sweep, confirming HAT maintains $>$80\% accuracy even at 15\% extreme mismatch. \\
\addlinespace
C2C Variability ($\sigma_{\mathrm{C2C}}$) & 2.0\% (Zhang 2025) & 1.0\% -- 8.0\% & \textbf{Low.} Barely affects final Top-1 accuracy within this range due to scale-masking. & Explained mechanistically in the main text via the scale-masking effect and quantified in Table~\ref{tab:sensitivity}. \\
\addlinespace
Retention ($\tau$, $A_0$) & $\tau=140/610$ms & $\pm 50\%$ & \textbf{Medium.} Affects long-term drift predictions. Extremely fast decay limits HAT recovery. & Sensitivity sweeps (Section~\ref{subsec:retention-sensitivity}) confirm conclusions remain stable over $\pm 50\%$ variations. \\
\addlinespace
Write Non-linearity ($NL$) & 2.0 (Proxy scaled) & 1.0 -- 3.0 & \textbf{High.} A sharp accuracy drop exists near $NL=2.0$. & Supplementary Fig.~\ref{fig:supp-nl-gradient} localizes the present surrogate failure to the MLP analog path, clarifying that this is a training-approximation bottleneck rather than a uniform forward-path collapse. \\
\bottomrule
\end{tabular}
}
\end{table}

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.88\textwidth]{fig_nl_gradient_distortion}
    \caption{\textbf{Group-wise gradient distortion under severe nonlinear write.} A deterministic diagnostic on the frozen Tiny-ViT V4 checkpoint compares the \texttt{NL=1.0} baseline against matched forward passes where \texttt{NL=2.0} is activated only for one analog module group at a time. Results aggregate eight CIFAR-10 train batches with preserved checkpoint D2D buffers and $\sigma_{\mathrm{C2C}}=0$ so that only the backward surrogate changes.}
    \label{fig:supp-nl-gradient}
\end{figure}

\textit{Interpretation.} Because the present \texttt{NL} implementation perturbs only the backward surrogate, this diagnostic isolates where the training signal is distorted without changing the forward loss. The result is highly structured rather than uniform: activating \texttt{NL=2.0} only in the MLP blocks drives the affected-parameter gradient cosine down to \texttt{0.815} and the norm ratio down to \texttt{0.671}, while patch embedding, attention QKV, and attention projection remain effectively unchanged at \texttt{1.00}. The all-analog setting matches the MLP result almost exactly (\texttt{0.816}), indicating that the current nonlinear-write bottleneck is dominated by the MLP analog path rather than by a transformer-wide failure. At the same time, the mean loss delta stays numerically zero across all groups, confirming that this is a backward-surrogate distortion effect rather than a forward-path mismatch.

In summary, the core conclusions regarding deployment risk ranking remain qualitatively consistent within a reasonable $\pm 50\%$ fluctuation of the proxy parameters. This supports the framework's use as a materials-to-system decision bridge. It is not intended to be pulse-accurate, but it still identifies the main architectural bottlenecks and operating boundaries.


\begin{table}[ht]
    \centering
    \caption{\textbf{Group-wise nonlinearity ablation under global $NL=2.0$.} Each row reports the best and final test accuracy when the specified analog module group is protected at $NL=1.0$ while all other analog layers remain at $NL=2.0$. The baseline $NL=1.0$ row uses the canonical uniform-noise HAT configuration. $^\dagger$Earlier diagnostic; the revised gradient-scaling recipe recovers the unmitigated $NL=2.0$ baseline to the $\sim$80--82\% band. $^\ddagger$Earlier diagnostic only; these groupwise protected results used an earlier gradient-scaling recipe. They are retained solely as source-domain gradient-diagnostic evidence and must not be used to claim MLP-path localization under the revised recipe.}
    \label{tab:supp-nl-ablation}
    \small
    \setlength{\tabcolsep}{5pt}
    \begin{tabular}{@{}lccc@{}}
        \toprule
        Protected group & Best acc (\%) & Final acc (\%) & $\Delta$ vs $NL=2.0$ (pp) \\
        \midrule
        (a)~Baseline ($NL=1.0$ global) & $87.95\pm0.27$ & — & $+60.23$ \\
        (b)~None ($NL=2.0$ global) & $27.72\pm0.82^\dagger$ & $27.72\pm0.82^\dagger$ & $0.00$ \\
        (c)~MLP only (fc1, fc2) & $87.79^\ddagger$ & $86.22^\ddagger$ & $+60.07$ \\
        (d)~QKV only (attn.qkv) & $18.72^\ddagger$ & $10.15^\ddagger$ & $-9.00$ \\
        (e)~Attn proj only (attn.proj) & $18.86^\ddagger$ & $\sim$10.25 (stopped @ ep 54)$^\ddagger$ & $-9.14$ \\
        (f)~All linear ($NL=1.0$ all analog) & $87.49^\ddagger$ & $84.81^\ddagger$ & $+59.77$ \\
        \bottomrule
    \end{tabular}
\end{table}

\textit{Interpretation.} The ablation shows that the MLP channel-mixing path is the dominant recoverable failure site under the \emph{earlier} $NL=2.0$ surrogate: linearizing only the MLP layers recovers accuracy to within $0.16$~pp of the full-linear configuration. By contrast, both attention-side linearizations (QKV and projection) collapse structurally and degrade below even the unmitigated baseline, indicating that the attention nonlinearity is required for representational geometry under the earlier surrogate. \textbf{All groupwise protected rows (c)--(f) are earlier diagnostics only.} They are retained here solely as source-domain gradient-diagnostic evidence from an earlier gradient-scaling recipe. Under the revised gradient-scaling recipe, fresh-instance transfer recovers to the $\sim$80--82\% band regardless of which path is protected, so the earlier MLP-path localization claim does \emph{not} carry over to the revised recipe. These results are therefore presented as a supplementary ablation rather than a main-text contribution.

\begin{table}[ht]
    \centering
    \caption{\textbf{Intermediate nonlinearity interpolation (R10D).} Canonical Ensemble HAT training with seed~123 and uniform noise at intermediate $NL$ values. Each row reports source-domain best accuracy and fresh-instance mean ($\pm$ std) over 10 fresh D2D arrays with 5 Monte Carlo passes per array. The $NL=1.0$ canonical value is the three-seed aggregate ($86.16\pm0.19$\%); the $NL=2.0$ value is the M-series Ensemble HAT mean ($\sim$80.54\%).}
    \label{tab:r10d-nl-interpolation}
    \small
    \begin{tabular}{@{}lccc@{}}
        \toprule
        $NL_{\text{LTP}}$ / $NL_{\text{LTD}}$ & Source best (\%) & Fresh mean (\%) & Fresh std (\%) \\
        \midrule
        $1.0$ / $-1.0$ (canonical) & $91.94$ & $86.37$ & $1.54$ \\
        $1.2$ / $-1.2$ & $83.12$ & $83.03$ & $0.22$ \\
        $1.5$ / $-1.5$ & $82.81$ & $82.63$ & $0.10$ \\
        $1.8$ / $-1.8$ & $82.77$ & $80.31$ & $0.40$ \\
        $2.0$ / $-2.0$ (severe) & $\sim$81.0 & $\sim$80.54 & $\sim$0.6 \\
        \bottomrule
    \end{tabular}
\end{table}

\textit{Interpretation.} The degradation from $NL=1.0$ to $NL=2.0$ is gradual: accuracy remains stable at $82.6$--$83.0$\% through $NL=1.5$, then drops by roughly $2.3$~pp between $NL=1.5$ and $NL=1.8$, before settling in the $\sim$80--82\% band at $NL=2.0$. This monotonic trend supports the main-text framing that the residual gap at severe $NL$ reflects training-recipe constraints in the strongly nonlinear regime rather than a sudden physical collapse.

\paragraph{Supplementary Note~S1. Cross-framework subset-evaluation protocol.}
Because a single noisy evaluation on the full CIFAR-10 test set would extrapolate to roughly 15~h per condition for CrossSim (5,361~s per 1,000-image run) versus $\sim$1~s per 1,000-image run for the native framework, the shared-regime sanity check was performed on a deterministic 1,000-image slice of the canonical CIFAR-10 test ordering (the first 1,000 images; no RNG seed was set). This slice yields an approximately balanced class distribution (86--112 images per class). The clean baseline was evaluated once per framework; the noise-injection settings were evaluated with three independent Monte Carlo draws. For the single-run clean baseline, the Wilson 95\% confidence intervals on the subset proportion are [83.9\%, 88.2\%] for our framework and [81.3\%, 85.9\%] for CrossSim. For the three-run noise condition, the t-distribution 95\% CIs (df = 2, $t_{0.975} = 4.303$) are [79.9\%, 83.3\%] for our framework and [59.1\%, 75.3\%] for CrossSim. The large CrossSim run-to-run spread ($\sigma_{\text{pop}} = 2.67$\%) reflects the sensitivity of its generic programming-error/read-noise mapping to the particular stochastic sample. Independently of this subset check, the canonical fresh-instance collapse of standard HAT was re-evaluated under FP32 inference with autocast disabled and remained exactly $10.00\pm0.00$\% across 10 fresh D2D instances with 5 Monte Carlo evaluations per instance, confirming that the collapsed single-class predictor is a deterministic training outcome rather than a numerical artifact (\texttt{fresh\_instance\_eval\_v4\_standard\_noamp.json}).

\paragraph{Supplementary Note~S2. Correlated-D2D stress test.}
% Canonical protocol: all numbers in this Note are from evaluation-only runs
% (NL=1.0, AMP disabled) on the canonical Ensemble HAT checkpoint
% `V4_hybrid_standard_noise_hat_best.pt`. No retraining involved.
A full $10\times5$ fresh-instance sweep under separable AR(1)-style spatial correlation across the effective crossbar grid yields:
\begin{itemize}
    \item \textbf{i.i.d.\ baseline} ($\rho=0$): $86.33 \pm 1.61$\% (10 fresh instances, 5 MC runs each);
    \item \textbf{$\rho=0.3$}: $84.57 \pm 2.39$\%, a $-1.76$~pp excursion versus i.i.d.;
    \item \textbf{$\rho=0.5$}: $82.12 \pm 3.95$\%, a $-4.20$~pp excursion versus i.i.d.
\end{itemize}
Rank ordering is preserved across all tested levels, with no instance collapsing below $73.7$\% (instance~4, $\rho=0.5$). The canonical Ensemble HAT checkpoint therefore tolerates moderate spatial correlation without catastrophic failure, although stronger correlation does produce a measurable degradation consistent with increased effective mismatch variance. Source: \texttt{fresh\_instance\_eval\_v4\_ensemble\_correlated\_d2d.json}.

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.72\textwidth]{figS_corr_d2d}
    \caption{\textbf{Fresh-instance robustness under spatially correlated D2D mismatch.} Error bars show cross-instance standard deviation over 10 fresh arrays, with 5 Monte Carlo evaluations per array, for the canonical Tiny-ViT V4 Ensemble HAT checkpoint. The i.i.d.\ baseline reaches $86.33\pm1.61$\%, while separable AR(1)-style correlation degrades the mean to $84.57\pm2.39$\% at $\rho=0.3$ and $82.12\pm3.95$\% at $\rho=0.5$. The dashed horizontal line marks the canonical standard-HAT collapse baseline (10.00\%) to emphasize that ranking is preserved despite bounded degradation.}
    \label{fig:supp-corr-d2d}
\end{figure}

\input{supplementary/S_theory_ensemble_hat}

\input{supplementary/S_mechanism_empirical}

\input{supplementary/S_hardware_calibration}

\input{supplementary/S_tooling_comparison}

\input{supplementary/S_reproducibility}

\subsection{Class-Distribution Analysis of the Fresh-Instance Collapse Mechanism}
\label{subsec:supp-class-distribution}

Figure~\ref{fig:supp-collapse-mechanism} compares per-class prediction distributions for the canonical fixed-mask Standard HAT checkpoint and the canonical Ensemble HAT checkpoint under fresh D2D instances at $NL=1.0$.

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.95\textwidth]{figS_standard_hat_collapse_mechanism}
    \caption{\textbf{Class-distribution diagnostic of the fresh-instance collapse mechanism.} Canonical fixed-mask Standard HAT evaluated on five fresh D2D instances at $NL=1.0$ remains at $10.00\pm0.00\%$ and assigns every CIFAR-10 test image to one class (prediction entropy $\approx0$, maximum-class frequency $=100\%$). Canonical Ensemble HAT under the same protocol reaches $85.97\pm1.98\%$ with near-uniform prediction entropy ($2.28$ vs.~$\ln 10$) and maximum-class frequency $15.27\pm2.27\%$.}
    \label{fig:supp-collapse-mechanism}
\end{figure}
