请直接在我上传的 PowerPoint 文件 `演示文稿1.pptx` 基础上重做，不要只微调文字。目标是把它改成一套适合组会/预答辩阶段展示的、明显更美观、更像高水平科研汇报的版本。

这是一个严格任务，不要自由发挥成通用商业模板。请按下面要求执行。

首先理解一个硬约束：

- 博士提供的原始 PPT / Word / 图片 / 未发表资料，不能直接作为最终汇报里的图件使用。
- 这些材料只能作为“参数来源、叙事参考、版式参考”。
- 最终 PPT 里能直接展示的图，必须优先来自：
  - 我们自己算出来的数据图
  - 我们论文/补充材料中已经生成的图
  - 我们自己制作的示意图
- 如果需要引用博士资料中的参数，只能做最小化标注或抽象说明，不能把原始图、原始表、完整参数页直接贴进去。

--------------------------------
一、先理解当前版本的主要问题
--------------------------------

当前这版最明显的问题不是“内容不够”，而是“信息密度分配错误、图表使用严重不足、页面视觉层级很弱”：

1. 12 页里有 7 页几乎是纯文字页。
   目前第 1、2、3、4、10、11、12 页基本没有真正的图/表支撑，只是大段 bullet。

2. 我已经上传了很多我们自己的论文图、补充图和生成素材，但当前稿件没有充分使用。
   你必须优先使用我们自己生成的真实图、真实表、真实数据图，不要再用抽象图标、默认形状、空洞示意图替代。

3. 当前页内经常是“标题 + 一大块文字 + 小图一张”，这很丑，也不适合科研汇报。
   需要改成“图表主导、文字服务于图表”。

4. 当前中英双语堆叠过多，像模板生成。
   可以保留英文副标题，但不能每页都做中英双份正文。中文为主，英文只做短副标题或术语补充。

5. 我不接受“数据讲了很多，但图上看不到”。
   关键数字必须和对应图/表放在同一页同一区域，不要只写在 bullet 里。

--------------------------------
二、硬性设计约束
--------------------------------

请严格遵守：

1. 版式比例保持 16:9。

2. 视觉风格必须像高水平科研组会/投稿前汇报，不要像：
   - 通用商业路演模板
   - 互联网运营汇报
   - AI 自动生成的默认紫色/蓝色大渐变模板

3. 风格建议：
   - 背景：浅色、克制、干净，优先米白 / 浅灰白，不要深色底。
   - 主色：深青绿 / 深蓝灰 / 暗橙作为强调色，最多 2 个强调色。
   - 字体：全篇统一，不要出现英文字体突然斜体、中文英文字体混乱的问题。
   - 不要使用花哨装饰、发光、厚阴影、随机圆角卡片堆叠。

3.1 字体和字号必须明确统一：
   - 中文主字体优先：`Source Han Sans SC` 或 `Microsoft YaHei`
   - 英文/数字优先：`Aptos` 或 `Arial`
   - 如果无法稳定混搭，就全篇统一用 `Source Han Sans SC` / `Microsoft YaHei`
   - 绝对不要出现随机斜体英文标题
   - 绝对不要一页一个字体系统
   - 字号建议：
     - 封面主标题：28–32 pt，Semibold
     - 正文页主标题：22–26 pt，Semibold
     - 英文副标题：10–12 pt，Regular，小写或 Title Case，不能过长
     - 正文 bullet / 说明：12–14 pt，Regular
     - 数字强调卡片：22–28 pt，Semibold
     - 图注 / 页脚注释：8.5–10 pt
   - 行距：1.10–1.20
   - 全部正文左对齐，不要两端强制对齐

3.2 文字样式必须统一：
   - 页标题统一不加句号
   - 中英文之间留正常空格
   - `Tiny-ViT`、`ConvNeXt`、`ResNet-18`、`ADC`、`D2D`、`C2C` 这些术语写法全篇统一
   - 变量和物理量只在公式或参数框里出现，不要把整页正文写成术语堆砌

4. 每一页都必须有明确的视觉重心。
   除封面外，每页至少有一块“真实图 / 真实表 / 真实数据图”作为主体。

5. 任何关键结论页都不能只放 bullet。
   如果某页有结论，就必须搭配对应图或表。

6. 所有图片必须尽量放大，避免“明明有图但缩成邮票”。

7. 所有图题、注释、页码、坐标、数字不要互相遮挡。
   如果图内原始标签太密，就通过裁剪、留白、分栏、旁注解决，而不是硬挤。

8. 不要凭空重新绘制科学图，优先使用我上传的真实图件和真实表格。
   如果必须重排，请做“版式重构”，不要做“内容改写”。

9. 每页必须控制文字量：
   - 每页最多 3~4 条 bullet
   - 每条 bullet 最好不超过 28 个汉字或 1 行半
   - 如果某页需要超过 4 个信息点，请改成表格、数字卡片或图注

10. 每页必须有一句“take-away”式结论，放在标题下方或图旁边。
    这句结论要短、硬、像结论，不要像介绍性段落。

11. 数字一定要和对应图放在一起。
    不允许“图在左边，关键数值写在另一页”或“只在讲稿里说，不在页上出现”。

12. 科研规范硬约束：
    - 不允许直接粘贴博士原始图片、原始 PPT 截图、原始 Word 图件、原始未发表图表。
    - 不允许完整展示博士参数表、原始测量页面或未经公开的器件图。
    - 如果某页需要提到博士资料，只能：
      - 作为一句来源说明
      - 作为经过抽象后的参数标签
      - 作为“future measured-profile data needed” 的需求说明
    - 最终页面中出现的主体图，必须是我们自己的图。

--------------------------------
三、素材使用原则
--------------------------------

你必须优先使用我们自己已经生成的这些真实素材，而不是自己生成抽象图：

1. 论文图和补充图（优先使用）：
   - `paper/latex_gpt/figures/fig1_system_architecture.pdf`
   - `paper/latex_gpt/figures/fig2_weight_mapping.pdf`
   - `paper/latex_gpt/figures/fig4_accuracy_comparison.png`
   - `paper/latex_gpt/figures/fig5_hat_recovery.png`
   - `paper/latex_gpt/figures/fig_contour_map.png`
   - `paper/latex_gpt/figures/fig_sobol_sensitivity.png`
   - `paper/latex_gpt/figures/figS3_ensemble_hat.png`
   - `paper/latex_gpt/figures/fig_fresh_instance_ablation.png`
   - `paper/latex_gpt/figures/fig10_zero_shot_transferability.png`
   - `paper/latex_gpt/figures/fig11_energy_breakdown.png`
   - `paper/latex_gpt/figures/fig_nl_gradient_distortion.png`
   - `paper/latex_gpt/figures/figS2_nonideality.png`
   - `paper/latex_gpt/figures/figS1_asymmetry_concept.png`
   - `paper/latex_gpt/figures/fig3_snr_curves.png`
   - `paper/latex_gpt/figures/fig9_noise_sensitivity.png`

2. 我们自己的生成图片/增强图（可以使用）：
   - `report_md/images/Hybrid AnalogDigital Architecture (Fig 1 Enhancement).png`
   - `report_md/images/Ensemble Hardware-Aware Training Concept (Fig S3).png`
   - `report_md/images/Physical Non-Idealities in Memory Arrays (Fig S2).png`
   - `report_md/images/Differential Pair Asymmetry Concept (Fig S1).png`
   - `report_md/images/Graphical Abstract  TOC Image.png`
   - `report_md/images/a23_cifar10c_robustness.png`
   - `report_md/images/a23_delta_accuracy_heatmap.png`
   - `report_md/images/a23_snr_vs_intensity.png`

3. 以下资料只能作为参考，不可直接贴图到最终 PPT：
   - `report_md/d5mh00948k.pdf`
   - `report_md/41467_2025_66891_MOESM1_ESM.pdf`
   - `report_md/WQY-基于缺陷工程的易失性和非易失性 用于高效分类的光储备池计算-第8稿.docx`
   - `report_md/记忆类型可调的光电突触和存储器用于储备池计算-第8稿(1).pptx`

这些参考资料只能用于：

- 提炼一两句背景描述
- 提炼最少量的参数来源标注
- 帮助判断下一步最缺什么 measured-profile 数据

不能用于：

- 直接截图贴到最终页里
- 直接搬运原始器件图、原始表、原始性能曲线
- 完整展示未公开参数页

--------------------------------
四、分页重做要求（必须逐页执行）
--------------------------------

不需要强行保留 12 页。

建议做成 12–16 页。

如果 12 页装不下，就拆页；不要为了压页数把页面重新做回“文字墙”。

优先原则：

- 宁可 14 页图表清楚，也不要 12 页硬塞到难看。
- 每增加一页，都必须解决一个明确的信息拥挤问题。

建议结构如下，但你可以在保证逻辑完整和视觉质量的前提下微调页数：

- 封面
- 背景 / 动机
- gap 对比
- framework
- baseline / fragility
- HAT recovery
- ADC cliff
- Sobol ranking
- fresh-instance failure
- Ensemble HAT
- zero-shot transfer
- limitations
- takeaways
- next steps / discussion

下面是各主题页的具体要求：

第 1 页 标题页
- 不能只是文字。
- 用一张真正高质量的主视觉作为背景或右侧主图，优先：
  - `Hybrid AnalogDigital Architecture (Fig 1 Enhancement).png`
  - 或 `fig1_system_architecture.pdf`
- 标题、作者、日期做干净排版。
- 页脚不要放大段英文，不要放多余装饰。
- 目标：像正式学术报告封面，不像模板封面。
- 推荐主标题：`阶段性成果汇报`
- 推荐副标题：`面向有机光电器件的任务级行为模拟框架`
- 封面不要再出现一整行很长的英文介绍，只保留一句短英文副标即可。

第 2 页 研究背景与问题定义
- 当前是纯文字，必须重做。
- 不要直接使用博士的原始器件图或文献截图。
- 请优先使用我们自己的系统图、graphical abstract、或者自己重绘的简洁背景示意。
- 页面结构建议：
  - 左侧：1 张我们自己的示意图或系统图
  - 右侧：3 条关键背景结论 + 一个小关键词条
- 推荐 take-away：`器件已显示阵列化与多级调控潜力，但系统级部署瓶颈尚未被定量回答。`
- 页面文字只保留：
  - 有机器件优势
  - 阵列/批量化已有进展
  - 系统级风险尚未量化
- 其余信息交给图和参数框表达。

第 3 页 现有工作为什么还不够
- 当前仍然是纯文字，必须重做。
- 请把这一页改成“对比矩阵页”：
  - 一张清晰表格，对比 DNN+NeuroSim / MemTorch / CrossSim / AIHWKIT / 我们框架
  - 行维度包括：organic photoresponse、double-exponential retention、write nonlinearity、modern vision backbones、fresh-instance transfer
- 页面上可以有一个我们自己的小示意图或框架图辅助，不要只有表。
- 目标：这页一眼看出 gap，而不是靠读整段 bullet。
- 推荐 take-away：`关键缺口不是没有模拟器，而是缺少面向有机器件 + 现代视觉模型的系统级框架。`
- 对比矩阵必须清楚写出：
  - 是否支持 organic photoresponse
  - 是否支持 double-exponential retention
  - 是否支持 write nonlinearity
  - 是否支持 modern vision backbones
  - 是否支持 fresh-instance transfer 分析

第 4 页 First-Order Behavioral Simulation Framework
- 当前文字过多，图太弱。
- 这里必须用 `fig1_system_architecture.pdf` + `fig2_weight_mapping.pdf` 做主视觉。
- 版式建议：
- 上方或左侧放大系统框架图
- 右下或下方放 weight-mapping / profile schema 图
- 旁边只保留 3 条最关键方法点
- 不要把这页做成一大段“核心贡献”文字墙。
- 推荐 take-away：`我们的方法本质上是一个 profile-driven、first-order behavioral decision aid。`
- 3 条文字建议固定为：
  - `统一 profile 接口：photoresponse / retention / write NL / ADC / D2D / C2C`
  - `支持 ResNet-18、ConvNeXt-Tiny、Tiny-ViT-5M`
  - `面向部署风险评估，而非 chip-accurate 电路预测`

第 5 页 基线结果与部署脆弱性
- 用 `fig4_accuracy_comparison.png` 作为主图，放大。
- 旁边只放最关键的数字：
  - FP32 baseline
  - canonical noise drop
  - HAT partially recovers
- 如果空间允许，加一个小表格总结 Tiny-ViT / ConvNeXt / ResNet-18 的三组关键数字。
- 不要让图缩得太小。
- 推荐 take-away：`数字基线很高，但 canonical noise 会把系统精度直接打穿。`
- 这一页必须明确出现至少这些数字：
  - `Tiny-ViT: 98.06 / 86.94 / 97.97%`
  - `ConvNeXt: 90.74 / 64.12 / 33.22%*`
  - `ViT CIFAR-100: 86.94 → 44.06%`
  - `ConvNeXt CIFAR-100: 64.12 → 23.86%`

第 6 页 HAT Recovery 与 Cross-Dataset 表现
- 用 `fig5_hat_recovery.png` 作为主图。
- 必须把 Flowers-102 的 low-data boundary 作为单独 callout 做出来。
- 如果你能从上传文件里找到对应表格/结果，请加入一个简洁 side table。
- 这页重点是“恢复有效，但数据少时仍脆弱”。
- 推荐 take-away：`HAT 能显著恢复噪声损失，但 low-data regime 依然是边界。`
- 必须出现的数字：
  - `44.06 → 65.48`
  - `23.86 → 60.54`
  - `Flowers-102 V3: 4.81 ± 1.25%`
  - `Flowers-102 V4 HAT: 22.48 ± 1.82%`

第 7 页 ADC Cliff + Sobol 解析
- 如果一页太挤，请拆成两页：
  - 第 7 页：ADC cliff / contour
  - 第 8 页：Sobol ranking
- 使用：
  - `fig_contour_map.png`
  - `fig_sobol_sensitivity.png`
- 版式建议：
  - 左：contour map 大图
  - 右：Sobol bar chart + 关键数字框
- 必须把这些数明确可见地放出来：
  - Full grid: `S_ADC = 0.976`, `S_D2D = 0.018`
  - Operational regime: `S_D2D = 0.922`, `S_ADC = 0.041`
- 不要再只写成 bullet。
- 推荐 take-away：`先保住 6-bit ADC，再去优化 D2D；这就是设计优先级。`
- 页面上必须让 “6-bit cliff” 成为第一眼能看见的信息。

第 8 / 9 页 Fresh-Instance Transfer Failure & Ensemble HAT
- 如果空间允许可以合成一页；如果不允许，请拆成两页：
  - 一页专讲 standard HAT fresh-instance collapse
  - 一页专讲 Ensemble HAT 及频率消融
- 用 `figS3_ensemble_hat.png` 或 `Ensemble Hardware-Aware Training Concept (Fig S3).png` 作为主体。
- 如果布局允许，同时加入 `fig_fresh_instance_ablation.png` 的小面板。
- 必须让这两个数字非常醒目：
  - Standard HAT fresh-instance = `10.00%`
  - Ensemble HAT = `86.37 ± 1.54%`
- 这一页的视觉重心必须是“灾难性失败 vs 修复后的恢复”。
- 推荐 take-away：`问题不是噪声本身，而是 standard HAT 过拟合到了单一硬件实例。`
- 频率消融也要出现，至少列：
  - `Per-epoch: 88.41%`
  - `Per-batch: 86.16%`

第 10 页 Zero-Shot Transfer to Literature-Calibrated OPECT
- 使用 `fig10_zero_shot_transferability.png` 作为主图。
- 在旁边做一个小参数框，列出 OPECT case 的关键 profile 参数：
  - dynamic range
  - states
  - sigma_C2C / sigma_D2D
  - zero-shot accuracy
- 这一页不要只有一句“88.53%”，而是要把这个结果和 profile substitution 逻辑绑定起来。
- 推荐 take-away：`profile substitution 允许我们在硬件成阵列前先评估 literature-calibrated device risk。`
- 必须醒目展示：`88.53%`
- 参数框必须做成“最少量参数标签”，不要完整复刻来源文档中的参数页。

第 11 / 12 页 Current Limitations & Scope Boundaries
- 当前这页太像“把所有负面 bullet 堆一起”，很丑。
- 如果信息量大，请拆成两页，而不是硬塞。
- 请改成 2×2 的“真实证据面板”或两页分区面板：
  - `figS2_nonideality.png`
  - `fig_nl_gradient_distortion.png`
  - `fig11_energy_breakdown.png`
  - 一个小型 comparison table（CrossSim / AIHWKIT / current scope）
- 文字压缩成每格 1 句。
- 这页必须让人看到“局限在哪”，而不是只看大段文字。
- 推荐 take-away：`当前框架回答的是 deployment-risk ranking，不是 chip-accurate 物理极限。`
- 必须出现的限制数字：
  - `NL = 2.0 → 27.72 ± 0.82%`
  - `11.45× vs FP32` 但标明 `first-order upper bound`
- CrossSim / AIHWKIT 对比必须写成“小型验证/对照”，不能写成“全面物理验证”。

第 13 页 Core Takeaways
- 当前纯 bullet 太普通。
- 请改成“数字卡片 + 小图标注”的总结页：
  - 6-bit ADC threshold
  - 10.00% → 86.37 ± 1.54%
  - 88.53% zero-shot OPECT
  - 89.61% composite stress result
  - 27.72% at NL=2.0
- 可以引用前面图的局部 crop 做背景，但不要变成拼贴垃圾页。
- 要求高级、克制、强总结感。
- 推荐 take-away：`这项工作真正给出的，是一个器件到系统的风险排序框架。`
- 这页不能是 5 个长句 bullet，必须是“数字 + 简短结论”。

第 14 页 Next Steps & Discussion
- 当前还是纯文字，不行。
- 请改成：
  - 左侧：roadmap/timeline（器件数据、阵列级建模、joint calibration、sim-to-real）
  - 右侧：真正需要补的数据类型小表格
- 不要直接放博士的原始图或参数表。
- 如果一定要表现 future measured-profile 需求，请用我们自己的表格/框图总结“需要什么数据”，而不是展示来源文档截图。
- 这页要让导师看到“接下来具体缺什么数据、下一步怎么推进”，而不是泛泛而谈。
- 推荐 take-away：`下一阶段的关键不是再堆模拟，而是补 measured profile、阵列级建模和 sim-to-real 校准。`
- 右侧小表格请明确列出：
  - 需要的器件数据
  - 这些数据要解决什么问题
  - 补到后能验证哪条结论

--------------------------------
五、全局版式要求
--------------------------------

1. 整套 PPT 的图文比例要明显提高。
   目前最大问题是图不够。你必须主动把我上传的图、表、截图放进来。

1.1 这里的“图”特指：
   - 我们自己的论文图
   - 我们自己的补充图
   - 我们自己生成的示意图
   - 我们自己整理出来的小表格

   不包括博士原始图件截图。

2. 同一页最多保留 3~4 个 bullet，能表格化就表格化，能图示化就图示化。

3. 页内标题层级要统一：
   - 主标题
   - 小副标题（可选）
   - 图注/注释
   不要每页都像重新换了一套模板。

4. 所有数字要显眼，但不能做成低级“大红字海报风”。
   请用科学汇报风格的数字强调卡片。

4.1 数字强调卡片的样式要求：
   - 浅底色块 + 深色数字
   - 数字下面跟一行很短的解释
   - 不要用大面积纯红/纯绿
   - 不要把百分号和单位做得比数字更大

5. 不要忽略我上传的真实图表。
   但这里的“真实图表”仅限我们自己的图表。
   如果某一页没有用到我们自己的图/表，说明你没有完成任务。

--------------------------------
六、输出要求
--------------------------------

请输出修改后的 PPT，并确保：

1. 每页都已经完成重排，而不是只改 2~3 页。
2. 所有新增视觉元素优先来自我们自己的论文图、补充图和生成图。
3. 所有关键数据在对应页能直接看到。
4. 不允许把博士的原始图、原始表、原始参数页直接塞进最终 PPT。
5. 如果某个位置你找不到最合适的我们自己的图，也必须留出一个清晰、漂亮、可替换的位置框，并在备注里写明：
   - 这里建议后续替换成哪类“我们自己生成的图”
   - 需要基于哪些参数或结果再做图
6. 如果你需要引用博士资料，请只在备注中写“参数来源参考了外部未公开资料”，不要在页内完整展示。

一句话概括你的任务：

把当前这份“文字太多、图太少、像自动生成模板”的 PPT，重做成一套“图表主导、数据充分、视觉克制、适合高水平科研组会汇报”的版本；优先使用我们自己的图表和示意图，不要直接使用博士原始图件；外部资料只能做参数来源和需求定义参考。
