# Codex Dispatch #9 — PROJECT_INDEX verification + final paper/ tidy + git hygiene audit

**Date:** 2026-04-17
**From:** Claude
**To:** Codex
**Upstream:** Commit `a7fa088` (`_archive/` consolidation + `PROJECT_INDEX.md`). Review `compute_vit/PROJECT_INDEX.md` first — it is the reference for every decision below.

**Scope:** verification, file moves, and classification only. **No code edits, no content edits, no `git commit`, no `git push`.** Same reversibility contract as Dispatches #7 and #8.

---

## TX-31 — Verify `PROJECT_INDEX.md` against reality

Walk every row in sections §3–§10 of `compute_vit/PROJECT_INDEX.md` and confirm:

1. The path exists.
2. The category/status label is defensible.
3. The stated invariants in §12 still hold (`find run_*.py -maxdepth 1 | wc -l` == 44; main.pdf/supp/cover page counts; single `.bib`).

**Deliverable:** `report_md/_gpt/PROJECT_INDEX_AUDIT_20260417.md` with:
- A short ✅/⚠️/⛔ per section.
- Any row where the path is wrong, the label is wrong, or a file was missed.
- Recommended diffs against `PROJECT_INDEX.md` (do NOT apply them — Claude applies).

Hard budget: 90 minutes. If this is taking longer, report what you covered and stop.

---

## TX-32 — Finalize `paper/` tidy (TX-25 follow-through)

TX-25 in Dispatch #8 was partially blocked because you found cross-refs. With `PROJECT_INDEX.md` now marking `paper/0[1-8]_*.md` + `paper/PAPER_OUTLINE.md` as **draft-superseded**, re-run the cross-ref check with the explicit whitelist below and move what is safe.

**Candidates to move to `_archive/paper-drafts/`:**
- `paper/01_introduction.md` → `_archive/paper-drafts/`
- `paper/02_related_work.md`
- `paper/03_methodology.md`
- `paper/04_experimental_setup.md`
- `paper/05_results.md`
- `paper/06_discussion.md`
- `paper/07_conclusion.md`
- `paper/PAPER_OUTLINE.md`
- `paper/BANANA_JOURNAL_SCHEMATIC_PROMPTS_20260408_gpt.md`
- `paper/NANOBANANA_SCHEMATIC_PROMPTS_gpt.md`
- `paper/PERPLEXITY_TARGETED_CITATION_PROMPTS_gpt.md`
- `paper/FIG1_FIG2_BRIEF_gpt.md`
- `paper/FIGURE_CAPTION_DRAFTS_gpt.md`
- `paper/参考文献库.md`

**Cross-ref check protocol** (STOP and record if ANY hit outside the whitelist):
```
for f in <candidates>; do
  grep -rln "$(basename "$f")" \
    --exclude-dir=_archive \
    --exclude-dir=report_md \
    --exclude-dir=.git \
    --exclude-dir=__pycache__ \
    compute_vit/
done
```

Acceptable hits (whitelist — still MOVE even if referenced here):
- Any file under `_archive/`
- Any file under `report_md/` (including `report_md/_gpt/`)
- `paper/PAPER_OUTLINE.md` itself when referenced by another paper/ .md being moved (self-consistent — both move together)

Blocking hits (STOP that file):
- Any `.py` that imports or opens the file
- Any `.tex`, `.bib`, `.sh` that references it
- Any live `.md` under `paper/latex_gpt/`

**KEEP in `paper/` (never archive):**
- `paper/CANONICAL_RESULT_LOCK_gpt.md` — numeric source of truth
- `paper/FIGURE_CAPTION_LOCK_gpt.md` — caption source
- `paper/FIGURE_PLAN.md` — referenced by `plot_paper_figures.py`
- `paper/08_appendix.md` — still regenerated by `make_appendix.py`. Confirm this by grepping `make_appendix.py` for `08_appendix` — if it writes there, KEEP it.
- `paper/figures/`, `paper/latex_gpt/`
- `paper/fix_plots.py`, `paper/generate_schematic_figures_gpt.py`, `paper/plot_paper_figures.py`

**Deliverable:** the moves themselves, plus a row in the audit file (TX-31 deliverable) under a `## TX-32` heading listing src → dst → grep result.

---

## TX-33 — Git hygiene audit (report only, no staging)

`git status --short` currently shows ~252 untracked and ~68 unstaged-modified files inside `compute_vit/`. This is the residue of sessions that never committed their code. **Classify** every entry into one of three buckets. **Do not** `git add`, `git commit`, or modify `.gitignore` in this pass — just produce the ledger.

**Buckets:**
1. **TRACK** — should be in the next commit (e.g., `README.md`, `LICENSE`, `EXPERIMENT_PROTOCOL.md`, `RELEASE_CHECKLIST.md`, any `run_*.py` that appears in the 44-whitelist in `PROJECT_INDEX.md §4.3`, legitimate code additions).
2. **IGNORE** — should be appended to `.gitignore` (build artifacts, caches, scratch JSON, generated figures that are re-derivable, `.vscode/`, `auto_fitted_profile.json`).
3. **ARCHIVE** — should move to `_archive/` per existing rules (one-shot scripts, dated one-off shell queues, obsolete markdowns).

**Deliverable:** `report_md/_gpt/GIT_HYGIENE_LEDGER_20260417.md` with three tables (one per bucket), each row: path | reason | suggested action. Also count each bucket. **Stop after writing the ledger** — Claude reviews and picks what to execute.

**Check before classifying `.py` files:**
- Is it in the 44-`run_*.py` whitelist (§4.3 of `PROJECT_INDEX.md`)? → TRACK
- Is it listed in `_archive/scripts-oneshot/` or `_archive/scripts-versions/` already? → it's not untracked; skip
- Does it have a `v2`/`v3`/`FIXED`/`fixed`/`_final` sibling that IS in the whitelist? → ARCHIVE the older one
- One-shot `.sh` with a date stamp (`run_..._20260417.sh`)? → ARCHIVE

---

## Hard constraints (identical to #7, #8)

- **No deletions.** All moves reversible via `mv`.
- **No content edits** to `.md`, `.tex`, `.py`, `.bib`, `.json` — except the three deliverable files TX-31, TX-32 audit append, TX-33 ledger.
- **No `git add`, `git commit`, `git push`, no `.gitignore` edits.**
- **No touching** `checkpoints/`, `data/`, `数据_博士/`, `paper/latex_gpt/sections/*.tex`, `paper/latex_gpt/*.pdf`, `paper/latex_gpt/*.bib`, `paper/latex_gpt/figures/`.
- **When in doubt, STOP and record.** Bias: over-keep.
- If a grep check reveals a blocking reference, leave the file in place and note the caller.

---

## Reporting

Update `CLAUDE_TASK_gpt.md` TX-31/32/33 status rows to `✅` / `⛔`. Append one block to `AGENT_SYNC_gpt.md` summarizing scope + deliverable paths + counts (audit issues found, files moved, ledger bucket sizes).
