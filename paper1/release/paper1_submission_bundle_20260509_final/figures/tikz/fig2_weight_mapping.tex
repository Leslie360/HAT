\documentclass[tikz,border=12pt]{standalone}
\usepackage{amsmath,amssymb}
\usepackage{mathptmx}
\renewcommand{\familydefault}{\rmdefault}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,calc}

\definecolor{lineColor}{RGB}{50,50,50}
\definecolor{boxBg}{RGB}{250,250,250}
\definecolor{accentBlue}{RGB}{0,114,178}
\definecolor{accentRed}{RGB}{213,94,0}

\begin{document}
\begin{tikzpicture}[
  font=\rmfamily,
  stagebox/.style={draw=lineColor, fill=boxBg, line width=1.5pt, rounded corners=6pt, inner sep=8pt, align=center, text width=3.5cm, minimum width=3.8cm, minimum height=4.5cm},
  arrowstyle/.style={->, >=Stealth, line width=2.0pt, draw=lineColor},
  steplabel/.style={font=\large\bfseries, text=lineColor},
  mathlabel/.style={font=\large, text=lineColor},
]

\def\dx{6.5}
\def\dy{-6.5}

% 1. Input
\node[stagebox] (S1) at (0, 0) {
  \textbf{\large Software Weight}\\[10pt]
  \begin{tikzpicture}[scale=0.5]
    \foreach \r in {0,1,2,3} {
      \foreach \c in {0,1,2,3} {
        \pgfmathsetmacro{\pct}{int(min(100, 30 + 15*\r + 12*\c))}
        \fill[black!\pct] (\c*0.7, -\r*0.7) rectangle + (0.6, 0.6);
      }
    }
  \end{tikzpicture}\\[10pt]
  $W$ (FP32)
};
\node[steplabel, above=0.3cm] at (S1.north) {1. Input};

% 2. Quantize
\node[stagebox] (S2) at (\dx, 0) {
  \textbf{\large Quantization}\\[10pt]
  \begin{tikzpicture}[scale=0.5]
    \foreach \r in {0,1,2,3} {
      \foreach \c in {0,1,2,3} {
        \pgfmathsetmacro{\val}{int(2+1.5*\r+1.2*\c)}
        \pgfmathsetmacro{\pct}{int(100*min(1,\val/15))}
        \fill[black!\pct] (\c*0.7, -\r*0.7) rectangle + (0.6, 0.6);
      }
    }
  \end{tikzpicture}\\[10pt]
  $W_q$ (4-bit)
};
\node[steplabel, above=0.3cm] at (S2.north) {2. Quantize};
\draw[arrowstyle] (S1.east) -- (S2.west) node[midway, above, mathlabel] {4-bit};

% 3. Map
\node[stagebox] (S3) at (2*\dx, 0) {
  \textbf{\large Diff.\ Pair}\\[10pt]
  $G^+_{ij} = g_0 + \alpha q_{ij}$\\[6pt]
  $G^-_{ij} = g_0 - \alpha q_{ij}$\\[10pt]
  $G^+ \!- G^- \propto W_q$
};
\node[steplabel, above=0.3cm] at (S3.north) {3. Map};
\draw[arrowstyle] (S2.east) -- (S3.west) node[midway, above, mathlabel] {encode};

% 4. Noise
\node[stagebox, draw=accentRed, line width=2.0pt] (S4) at (3*\dx, 0) {
  \textbf{\large Device Noise}\\[10pt]
  $W_{\mathrm{eff}} = W_q \!\cdot\! G$\\[6pt]
  $\odot(1 \!+\! M_{\mathrm{D2D}})$\\[6pt]
  $+ \,\xi_{\mathrm{C2C}}$\\[10pt]
  \textcolor{accentRed}{$\sigma_{\mathrm{D2D}}\!=\!0.10$}\\[4pt]
  \textcolor{accentRed}{$\sigma_{\mathrm{C2C}}\!=\!0.05$}
};
\node[steplabel, above=0.3cm] at (S4.north) {4. Noise};
\draw[arrowstyle] (S3.east) -- (S4.west) node[midway, above, font=\large\bfseries, text=lineColor] {D2D+C2C};

% 5. Compute
\node[stagebox] (S5) at (3*\dx, \dy) {
  \textbf{\large Analog MAC}\\[10pt]
  $I_{\mathrm{out}} = W_{\mathrm{eff}} \cdot x$\\[10pt]
  Crossbar\\[6pt]
  \textcolor{accentBlue}{$\mathcal{O}(n^2)$ in $\mathcal{O}(1)$}
};
\node[steplabel, below=0.3cm] at (S5.south) {5. Compute};
\draw[arrowstyle] (S4.south) -- (S5.north) node[midway, right=0.2cm, font=\large\bfseries, text=lineColor] {$V_{\mathrm{in}}$};

% 6. Digitize
\node[stagebox] (S6) at (2*\dx, \dy) {
  \textbf{\large ADC}\\[10pt]
  $I_{\mathrm{out}} \rightarrow d_{\mathrm{out}}$\\[10pt]
  6-bit\\[6pt]
  DNL $\approx$ 0.5 LSB
};
\node[steplabel, below=0.3cm] at (S6.south) {6. Digitize};
\draw[arrowstyle] (S5.west) -- (S6.east) node[midway, above, mathlabel] {$I_{\mathrm{out}}$};

% 7. Recover
\node[stagebox] (S7) at (\dx, \dy) {
  \textbf{\large Scale Recovery}\\[10pt]
  $y = \frac{d_{\text{out}}}{G_{\mathrm{ref}}} \cdot s$\\[10pt]
  Calibrated\\[6pt]
  Activation
};
\node[steplabel, below=0.3cm] at (S7.south) {7. Recover};
\draw[arrowstyle] (S6.west) -- (S7.east) node[midway, above, mathlabel] {$d_{\mathrm{out}}$};

% 8. Output
\node[stagebox, draw=accentBlue, line width=2.0pt, fill=white] (S8) at (0, \dy) {
  \textbf{\large Output Activation}\\[10pt]
  $y$\\[10pt]
  (Digital Domain)
};
\node[steplabel, below=0.3cm] at (S8.south) {8. Output};
\draw[arrowstyle] (S7.west) -- (S8.east) node[midway, above, font=\large\bfseries, text=lineColor] {$y$};

\end{tikzpicture}
\end{document}
