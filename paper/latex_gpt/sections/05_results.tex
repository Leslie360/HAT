% !TeX root = ../main.tex
\section{Results}
\label{sec:results}

\subsection{The Failure of Pure Quantization and Algorithmic Rescue}
\label{subsec:failure-rescue}

We first measure the behavior of the tested AIHWKit IdealDevice quantization/noise baseline. At 8-bit precision the baseline remains stable, achieving 87.28 $\pm$ 0.13\% fresh-instance accuracy; at 4-bit precision it collapses to 14.64 $\pm$ 0.11\%. This indicates that standard fixed-instance hardware-aware training fails to generalize under fresh hardware instance shifts at extreme low precision in this tested regime.

To address this, we apply Ensemble Hardware-Aware Training (HAT). By resampling the device-to-device (D2D) variation patterns during the forward pass, Ensemble HAT rescues the tested 4-bit pure-quantization regime where the AIHWKit IdealDevice baseline collapses. The model recovers to 86.16 $\pm$ 0.19\% fresh-instance accuracy, effectively closing the gap with the 8-bit baseline and demonstrating robust cross-instance transfer.

\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{fig1_paper1_spine}
\caption{\textbf{Paper-1 experimental spine.} (A) Standard fixed-mask HAT optimizes one device instance, whereas Ensemble HAT trains over resampled D2D masks. (B) In the idealized AIHWKit baseline, 4-bit fresh-instance accuracy collapses while Ensemble HAT recovers it. (C) Under PCM UnitCell simulation, 4/6/8-bit models all train, but 4-bit has larger one-day drift and 6-bit is the best tested precision-retention midpoint.}
\label{fig:paper1-spine}
\end{figure}

\subsection{Realistic PCM Training and the Precision-Retention Frontier}
\label{subsec:pcm-precision-ladder}

While Ensemble HAT addresses the algorithmic generalization problem in a pure-quantization context, realistic analog deployment must account for complex device physics, particularly conductance drift. We evaluate our approach using the \texttt{PCMPresetUnitCell} model in AIHWKit, which incorporates non-linear update physics, finite dynamic range, and time-dependent drift.

Under the tested PCM UnitCell simulation regime, training is viable across 4-bit, 6-bit, and 8-bit precisions. However, the results reveal a precision-retention deployment frontier rather than a single best compression point. Figure~\ref{fig:paper1-decision-map} converts the result into a deployment decision map: distribution-level HAT fixes the fresh-instance failure, while PCM precision selects the retention budget.

\begin{figure}[H]
\centering
\makebox[\textwidth][c]{\includegraphics[width=1.16\textwidth]{fig2_paper1_decision_map}}
\caption{\textbf{Results-to-deployment decision map.} (A) Fixed-mask 4-bit HAT collapses under fresh hardware instances, while D2D-resampled Ensemble HAT restores 4-bit fresh accuracy near the 8-bit idealized baseline. (B) In PCM UnitCell simulation, 8-bit and 6-bit are drift-flat over one day, whereas 4-bit is trainable but drift-limited. (C) The resulting deployment rule is sequential: diagnose fresh-instance collapse, train with a mismatch-distributed objective, then choose the PCM precision from the retention budget.}
\label{fig:paper1-decision-map}
\end{figure}
\FloatBarrier


\subsection{Deployment Guidance}
\label{subsec:deployment-guidance}

The precision ladder separates the algorithmic challenge of cross-instance robustness from the device-level precision-retention tradeoff. 8-bit PCM is the drift-flat reference; 4-bit PCM is trainable but loses $\sim$4.01~pp over one day; 6-bit is the best observed midpoint in the tested PCM UnitCell setting, with 77.86 $\pm$ 0.56\% fresh accuracy and only 0.10~pp one-day drift.

\subsection{Future Directions}
\label{subsec:future-directions}

Extending the same hardware-aware training principles to memory-bound inference components such as analog KV-cache is an important future direction.
