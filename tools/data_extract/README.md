# tools/data_extract/

Reusable data extraction/conversion helpers.

No active scripts are currently stored here.

## Rules

- Helpers must state raw input, derived output, and manifest path.
- Derived data promoted to Paper1/Paper2/thesis must be indexed in that product lane.
- Large raw data belongs in local data areas, not in release bundles.
